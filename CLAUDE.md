# FlowMoney — Hướng dẫn cho AI assistant

Ứng dụng quản lý tài chính cá nhân (đồ án tốt nghiệp). Flutter client (`src/Client-app`, Drift/SQLite, BLoC) ↔ Node/Express/Prisma/PostgreSQL (`src/Backend`). Kiến trúc **offline-first**: ghi vào SQLite trước, đồng bộ nền hai chiều sau.

---

## Đọc gì trước khi làm

**Luôn luôn:** `docs/PROJECT_CONTEXT.md` — đọc có trọng tâm, không đọc tuần tự:

1. **Mục 12** — Quy tắc phát triển bắt buộc
2. **Mục 14** — Trạng thái hiện tại (cái gì xong, cái gì dang dở)
3. **Mục 11** — Các lỗi đã sửa, đọc để **không lặp lại**
4. Mục 6 & 7 — chỉ khi đụng tới đồng bộ hoặc CSDL cục bộ

**Rồi tuỳ việc:**

| Việc | Đọc thêm |
|---|---|
| Làm tiếp phía client | `docs/CLIENT_APP_KNOWN_GAPS.md` — các mục dang dở kèm **lý do hoãn** và **bán kính ảnh hưởng** |
| Việc thuộc backend | **`docs/superpowers/backend/CAN-LAM/README.md`** — **cửa vào duy nhất** (mục 0 = việc còn xin; mục 1–2 = báo cáo backend, giữ nguyên văn; banner = bảng client soát). ✅ **Mục 0 nay RỖNG — không còn việc nào xin backend** (gộp `main` @ **`fcc20b5`** ngày 2026-09-13): backend báo **21/21 hoàn tất 100%** và chuyển nốt mục 21 `AI_EDGE_SLM_CLIENT_MISMATCH.md` sang `DA-XONG/`, nên `CAN-LAM/` chỉ còn `README.md`. ⚠️ Lần gộp ấy backend **viết lại toàn bộ** README (giảm 134 dòng), nên mọi dòng client từng chèn vào đó đã mất — đừng tìm lại, và đừng chèn tiếp: tệp ấy do backend quản. Cùng lượt gộp, `admin.service.js` đổi luật: admin **được phép xoá mềm danh mục hệ thống**, và tạo lại cùng tên thì **khôi phục** hàng đã xoá. Client **không phải sửa gì** — `getBackendDefaults` vốn lọc hàng đã xoá mềm, nên tài khoản mới thôi nhận bản sao ấy còn tài khoản đã seed giữ bản riêng; luật seeder đếm cả hàng đã xoá nên không tái hiện G16. *Bối cảnh mục 21, giữ vì nó ghi lại vì sao tài liệu AI có hình dạng hôm nay:* gộp `main` @ `eb071bb` (`04d1352`) mang về tệp **mới** `docs/AI/AI_Edge-SLM.md/Client-app.md` của NPBao: đặc tả **AI điều phối ngân sách on-device** cho Client-app (3 tầng, 39 luật A–H, 3 bảng SQLite cục bộ), tự xưng *"Nguồn sự thật"*. Client soát bằng mã và tìm ra **sáu** chỗ lệch — nặng nhất là **A3**, định nghĩa hoàn tiền bằng `amount < 0` trong khi **SQLite client lưu `amount` luôn dương** (chiều tiền ở `type`; `.abs()` ở cả đường ghi tay lẫn nhánh kéo về, dấu chỉ áp lúc đẩy), nên áp nguyên văn thì luật **không khớp hàng nào, im lặng** — cộng ba cột chưa tồn tại (`is_outlier`/`is_one_time`/`is_recurring_hint`), `saving_goal_ratio` chưa tồn tại (mục tiêu client là **số tiền đích**), `income` không lưu ở đâu, mô hình ngân sách rộng hơn giả định (`categoryId` nullable → có ngân sách tổng; kỳ không bắt buộc theo tháng; có `isExpired`), và **F2** hứa một lớp mã hoá client không có (`note` đi đồng bộ ở dạng **thô**) — cùng **bốn** chỗ tài liệu tự mâu thuẫn (8 nhóm chứ không 7; 3 bảng chứ không 2; `Project.md` thiếu C7/D5 và nhầm nhãn C5/C6). Tài liệu 21 **chỉ xin sửa chữ**, không đòi backend đổi mã, và **không chặn client** — tính năng Edge SLM chưa lên lịch làm. Cùng lượt, client sửa **hai chú thích sai của chính mình** ở `transactions_table.dart` (docstring bảng và cột `amount` vẫn nói *"Amount giữ dấu ±"*, tức mô tả cột PostgreSQL) — nhiều khả năng đó là nguồn gốc của A3. Nhánh này đã gộp `main` @ **`7779999`** ngày 2026-09-12 tối muộn (`e9b5aef`; trước đó `cbbeeb4` cùng ngày — `3bdac75`; `cc65f4f` ngày 2026-09-11) và CSDL dev đã áp tới `database/13` (tệp 12 ngày 2026-09-11 kèm `prisma generate`; tệp 13 ngày 2026-09-12, không cần `generate` vì `schema.prisma` không đổi). Backend báo **"19/19 hoàn tất 100%"** và chuyển nốt ba tài liệu 17, 18, 19 sang `DA-XONG/`; client soát lại bằng mã, CSDL **và máy ảo** cùng ngày (bảng ở **banner đầu README**): **17 A ✅ đã chạy thật** — `accountRejection` trả `null` khi hợp lệ, bắt tay socket **nối được** (`[RealtimeChannel] Đã nối` sau 51 lần bị từ chối), `/auth/refresh` **đã đo đầu-cuối chiều cùng ngày** (200; 401 + `code` khi khoá); **17 B một nửa** — chốt nhầm ở `upsertBill` đã bỏ nên **hoàn tác thanh toán hoá đơn đã đồng bộ lên được server** (đo thật: hàng kẹt 7 lần đẩy lên `Pending`), nhưng chốt xin đặt ở `upsertTransaction` **chưa có** — server nay **không chặn khoản chi thứ hai cùng `Idbill` ở đâu cả**, và `Rule_project.md:430`/`Backend.md:511` mô tả một chốt không tồn tại — ✅ **hết từ `7779999`** (gộp tối muộn cùng ngày): backend đặt `chanTraHaiLan` ở `upsertTransaction` (CAN-LAM 20 §2.1), client đo thật **4 ca** qua `/sync/push` với tài khoản thử 14 — lô thứ hai cùng `Idbill` bị `BILL_ALREADY_PAID`, xoá + trả lại **cùng lô** lọt, sửa chính khoản ấy lọt, hàng sống luôn = 1; hai câu tài liệu ấy nay đúng; **17 C ✅**; **18** còn **năm** việc mã (sổ ghi migration, `BLIND_INDEX_SECRET` không chốt, ca thử `WALLET_NAME_DUPLICATE`, `_mock*`, `'ORC'`) và **tám** chỗ tài liệu — hai lỗi **mới**: `New_Database.md:172,374` nói ví không có CHECK `Status` (có `chk_wallet_status`), `:396` bịa FK `auto_deposit_wallet_id → Wallet`; **19 ✅** — `GET /auth/profile` trả `countdown`, `pendingDeleteCancelled` gỡ sạch, và **client đã đọc** từ 2026-09-12 (`AuthRepositoryImpl._dongBoTrangThai` ghi số + mốc nhận kể cả khi trạng thái khớp — đúng §2.4 của tài liệu xin). Phần còn lại gom thành **mục 20** `CON_LAI_SAU_CBBEEB4.md` (2026-09-12; backend đóng và chuyển sang `DA-XONG/` tối muộn cùng ngày) — §2.1 chốt ở `upsertTransaction` là việc **duy nhất** chặn client (mở đồng bộ `Auto_pay`). ✅ **Backend làm xong cả mục 20, gộp `7779999` tối muộn 2026-09-12** — client soát cùng lượt: 2.1 đo thật 4 ca; 2.2 log dev in `[SECURITY] BLIND_INDEX_SECRET chưa đặt` khi nodemon nạp lại; 2.3 hai controller đòi `ALLOW_MOCK_INPUT === 'true'`; 2.4 `grep` **0** chỗ `'ORC'`; 2.7 mã đã gọi `getAccountValidity` ở nhánh token thu hồi (ca tài khoản còn `Active` đo thật vẫn 401 không mã — đúng; ca khoá/xoá **chưa đo đầu-cuối**, cần API admin); 2.5 **không kiểm được** — `Test/` bị `.gitignore`, không có trên máy; sổ ghi migration 5–13 có (kiểm 5, 6, 13 bằng chính câu kiểm của sổ) và tám chỗ tài liệu đã sửa. **Bước 12 (`Auto_pay` qua đồng bộ) không còn bị chặn — là việc phía client, chưa làm.** ⚠️ `.env.example` nay `NODE_ENV=production`, mà production **thiếu `BLIND_INDEX_SECRET` là từ chối khởi động** — máy mới dựng từ tệp mẫu sẽ crash. Bảng của backend ở mục 1–2 README đã thay bảng soát 15 tài liệu của client (2026-09-11); bảng soát mới ở banner đánh theo **số mục cũ** — tài liệu khác dẫn "CAN-LAM 13", "mục 11"… theo đúng số ấy. G24, G29, G30, G31, G32 đã đóng; việc từng chờ backend mà nay **chỉ còn phía client** (chưa làm — đừng viết tài liệu xin backend): năm cột hoá đơn server đã có (`Idbill`, `Previous_bill_id`, `Period_end`, `Auto_pay`, `Anchor_day`; **bốn** trong số ấy client **đã dùng** từ 2026-09-12 — `Period_end` tối cùng ngày, ân hạn hoá đơn v21; chỉ còn `Auto_pay`), `Pay_status = 'Skipped'` (✅ **client xong 2026-09-12**), mở `wallet.status` qua đồng bộ (G28, người dùng chốt để sau), nghe `sync.completed` (G34 — ✅ **client xong tối 2026-09-12**, im lặng, kiểm máy ảo hai máy). Tài liệu **đã đóng** nằm ở **`DA-XONG/`** (34 tệp + mục lục — đếm bằng máy 2026-09-12 — kèm ghi chú đóng bằng cách nào; chín trong số ấy còn việc dang dở, gom vào mục 18 `VERIFY_7675B35_REMAINING.md`, nay cũng ở đó) — mở khi cần biết *vì sao* lược đồ có hình dạng hôm nay, không phải khi tìm việc. Thư mục cha chỉ còn mục lục và **ba** tệp bối cảnh (đếm lại 2026-09-11) — trong đó `TRANSACTION_NOTE_ENCODING.md` (2026-09-08) **không xin gì**, chỉ báo rằng client mã hoá ý nghĩa vào `transaction.Note`. ⚠️ **`New_Database.md` nay ở `docs/Rule_Project/`**, nhánh `main` chuyển ngày 2026-09-10; mọi liên kết tương đối cũ trỏ vào `docs/superpowers/backend/New_Database.md` đều đã hỏng. Bảng trạng thái đầy đủ ở mục 14 `docs/PROJECT_CONTEXT.md` |
| Đụng vào đồng bộ | `src/Client-app/test/core/sync/sync_payload_contract_test.dart` — đọc **như tài liệu**, đây là nơi duy nhất ghi hợp đồng tên trường giữa hai phía. ⚠️ Và **một cam kết về THỨ TỰ** từ 2026-09-13: `SyncEngine` phát `pushResultStream` **TRƯỚC** bước Pull (`sync_push_result_truoc_pull_test.dart` canh). Người nghe phản ứng với *thất bại của lần đẩy*, và phản ứng ấy thường phải bù lại một thay đổi cục bộ máy này vừa ghi — Pull ở giữa đã thay đúng hàng ấy bằng bản của server, nên phép bù cộng vào con số **không chứa** thay đổi cần bù. Đã vấp thật: ví phình thêm đúng một lần trả hoá đơn, im lặng, trong khi 2303 ca test đều xanh. Kết quả của lần **thử lại** vẫn phát ở cuối — chính Pull là thứ làm nó đáng thử lại — nên người nghe phải chịu được **hai** lượt phát cho cùng một thất bại |
| Đụng vào hoá đơn | ⚠️ **Phép tính NGÀY của kỳ kế tiếp có MỘT định nghĩa từ 2026-09-16**: `kyKeTiepCua(Bill)` ở `features/bill/domain/bill_ky_ke_tiep.dart` (nối từ `periodEnd`, giữ `anchorDay`, cộng ân hạn). `BillRepositoryImpl._nextPeriodOf` **gọi lại** nó, và dự báo 30 ngày của trang Phân tích chiếu kỳ tương lai bằng đúng hàm ấy — nên hàng thật sinh ra khi trả tiền không bao giờ lệch với kỳ đã dự báo (có ca test canh hai bên). Chép lại phép ấy là tái hiện hai bẫy nó giữ. — `docs/bill/BILL_DOCUMENTATION.md` (⚠️ thư mục bị `.gitignore` chặn, chỉ có trên máy đã dựng) và **bộ test như tài liệu**: `test/features/bill/domain/bill_pay_status_test.dart` (**định nghĩa duy nhất** của trạng thái trả), `bill_status_test.dart` (**năm** trạng thái hiển thị + số liệu thẻ tổng), `bill_payment_test.dart` (trả theo số tiền kỳ này, hoàn tác), `bill_skip_test.dart` (bỏ qua kỳ và hoàn tác nó), `test/core/database/bill_overdue_test.dart` (cờ quá hạn đi **hai chiều**). **Sáu** thứ dễ vấp nhất: ngày đến hạn neo vào **ngày gốc** `Bills.anchorDay` (v18) chứ không suy từ ngày hiện tại — quy tắc "đoán cuối tháng" đã bỏ ngày 2026-09-08, và ba chốt chặn quanh nó ở mục "Ngày gốc" của tài liệu; bảng `Bills` mang **hai cặp cột trùng nghĩa** (`payStatus`/`isPaid` và `isRecurrence`+`timeRecurrence`/`recurrence`) — đọc và ghi theo cột **chính thức**, cột chuỗi cũ chỉ được suy ra; mỗi kỳ của hoá đơn lặp là **một hàng mới**, không phải một hàng sống lâu; và ✅ **ba cột nối nay ĐI QUA ĐỒNG BỘ từ 2026-09-12** — `transactions.billId` (khoá payload `idbill`), `bills.generatedFromBillId` (`previous_bill_id`), `bills.anchorDay` (`anchor_day`). ⚠️ Hai điều đi kèm, phá là hỏng **im lặng**: `transaction.Idbill` có khoá ngoại `fk_transaction_bill` nên **hoá đơn phải được đẩy TRƯỚC giao dịch** (mục 4 của `_collectPendingOps` đứng trước mục 5, có ca test canh thứ tự); và nhánh kéo về dùng `Value.absent()` khi server im lặng, vì hàng đã nằm sẵn trên server mang `NULL` cho tới khi client đẩy lại từng hàng — đọc thẳng là cắt chuỗi kỳ và xoá ngày gốc ngay chu kỳ pull đầu tiên. Hệ quả còn lại: hàng **cũ** vẫn trống ba cột ấy cho tới khi được sửa lại, nên hoàn tác liên máy chỉ chạy với hàng ghi từ 2026-09-12 trở đi. Và ✅ **trạng thái thứ năm `Skipped` — bỏ qua kỳ — có từ 2026-09-12** (mục **6.7** của tài liệu): câu "đã trả chưa" **tách làm hai** (*còn phải trả* vs *đã có khoản chi*) và cả hai có **một định nghĩa duy nhất** ở `features/bill/domain/bill_pay_status.dart` — bốn hằng chuỗi (bắt buộc, vì `getUpcoming`/`markOverdue` là SQL) cộng ba vị từ; trước đó biểu thức ấy bị chép tay ở **10** chỗ / 7 tệp và **ba** chỗ chỉ đúng do may. Ba chốt phá là hỏng im lặng: `payBill` **từ chối** kỳ `Skipped` (kỳ ấy đã sinh kỳ kế tiếp, trả tiếp là đẻ kỳ trùng), `undoSkip` **từ chối** kỳ đã trả (hoàn tác lần trả phải qua `undoPayment` vì chỉ nó mới hoàn tiền), và nhánh `skipped` của `billDisplayStatusOf` phải đứng **TRƯỚC** mọi phép so ngày. `undoSkip` **không** hoàn tiền — chép `undoPayment` sang là tặng tiền cho ví. Tab thứ hai của trang danh sách nay tên **"Lịch sử"**, và `BillSections` dùng `chuaDong`/`daDong`. Không đổi schema, không thêm trường đồng bộ. ✅ **`auto_pay` ĐI QUA ĐỒNG BỘ từ 2026-09-13** (bước 12 xong, payload hoá đơn **21** trường): công tắc tự trả nay là thuộc tính của *hoá đơn* chứ không của *máy*. Đi kèm nó là `BillPaymentConflictResolver` (mục **6.8** của tài liệu) — máy thua một cuộc đua nhận `BILL_ALREADY_PAID` thì tự gỡ khoản trả của mình và hoàn tiền. **Bốn chốt quanh nó, phá cái nào cũng hỏng im lặng, cả bốn đều do nghiệm thu hai máy ảo bắt được trong khi 2296 ca test đều xanh:** `undoPayment` phải nhận `transactionId` đích danh (`getByBill` là `LIMIT 1` không `ORDER BY`, đã gỡ nhầm khoản của máy thắng); phải `danhDauDaTra` trước `markSynced` (để `Pending` là bộ tự trả trả lại — đo 5 vòng trong 3 phút, ví phình 350.000 mỗi vòng); hoá đơn **KHÔNG** được `markSynced` mà phải đẩy `Payed` lên (server giữ `Pending` vĩnh viễn vì máy nào cũng bị pull ghi đè rồi đẩy bản cũ hơn); và `SyncEngine` phát kết quả đẩy **TRƯỚC** bước Pull (Pull ở giữa ghi đè số dư ví, nên phép hoàn tiền cộng vào con số không chứa lần trừ — ví phình). ⚠️ Còn **G37**: máy *thắng* giữ khoản chi hợp lệ mà ví không trừ nó, vì `wallets.balance` là cột LWW thuần và server không tính lại từ giao dịch. Và ✅ **ân hạn — `bill.Period_end` — xong 2026-09-12 tối, schema v21** (mục 2b `BILL_DOCUMENTATION.md`, spec `2026-09-12-bill-an-han-period-end-design.md`): ba mốc `startDate < periodEnd ≤ dueDate`, người dùng nhập **số ngày** (`0 · 7 · 15 · 30 · Khác`), hai ô ngày khoá; định nghĩa duy nhất ở `domain/bill_an_han.dart`; payload hoá đơn **20** trường. Thứ **sáu** dễ vấp: **kỳ sau nối từ `periodEnd ?? dueDate`** — nối từ `dueDate` là hở đúng số ngày ân hạn, mỗi kỳ trôi thêm, im lặng; và **`NULL` chỉ một nghĩa** (hàng cũ, ân hạn 0) — mọi đường ghi mới luôn ghi `periodEnd` kể cả khi bằng `dueDate`, nhánh kéo về `Value.absent()` như `anchor_day`. Quá hạn, nhắc, tự trả **vẫn neo `dueDate`** |
| Đụng vào thông báo | `docs/NOTIFICATION_FEATURE.md` — ✅ **KHOẢN CHI LỚN xong 2026-09-17** (loại thứ **18** của `enum NotificationKind` — hàng thứ **17** của bảng mục 3, hai con số cố ý khác nhau vì `billPaidOnOtherDevice` không nằm trong bảng ấy; mục **5f**; mục #7 của khảo sát lần hai — **không** nằm ở trang Phân tích). ⚠️ **"Bất thường" là NGƯỠNG người dùng đặt, không phải thống kê theo danh mục**: tài khoản thật có **8 ngày** dữ liệu và danh mục đông nhất chỉ **5** giao dịch (đo 2026-09-17), nên luật thống kê sẽ im hàng tháng rồi nổ bừa ngay khi vừa đủ mẫu — còn một con số do người dùng đặt thì **không thể báo động giả**. `nguongChiLon` mặc định **0 = tắt**, đúng khuôn `nguongSoDuThap` đến từng dòng. ⚠️ **Ba chốt hỏng im lặng**: "chi" phải đi qua **`khoanVaoThongKe`** chứ không phải `type == 'chi'` (khoản chuyển, điều chỉnh số dư, **mở sổ** — tạo ví 20 triệu không phải vừa chi 20 triệu); khoá là **`bigSpend:<idGiaoDich>`** và **cố ý không chở `walletId`** (chở nó thì đổi ví của một khoản chi đẻ thông báo thứ hai, nên deeplink là `/transactions` trần); và `createdAt` là **ngày giao dịch** — lấy mốc quét là vứt bỏ phép chặn `silenceBefore` ở lần bật đầu tiên. Xếp vào **nhóm Ngân sách**, không nhóm mới: nhóm riêng là chip thứ tám trên dải đã phải cuộn ngang. `loadChiLon` chỉ hỏi CSDL khi ngưỡng > 0. Không đổi schema, không thêm trường đồng bộ. — **trạng thái bàn giao, phần việc còn lại, và mười ba cái bẫy**. Mục 7 phải đọc trước khi đụng vào phần hệ điều hành; riêng **7.11** (`AndroidManifest.xml` là vùng mù của `flutter test`, `flutter analyze` **và** `flutter build apk`) phải đọc trước khi đụng vào nút hành động hay lịch đặt trước; **7.13** phải đọc trước khi viết widget test cho trung tâm thông báo — thiếu `dongTrang()` là **treo cả tệp**, không phải một test đỏ. Bảng thông báo **cục bộ**, không nằm trong `SyncEntityType`. Loại thứ **18** là **Khoản chi lớn** (2026-09-17, mục **5f**); loại thứ **16** là **Tổng kết tuần** (2026-09-09, mục **5d**): nhóm riêng `summary`, công tắc **mặc định tắt**, giờ do người dùng chọn, và số tuần ISO **tự viết** ở `tuan_iso.dart` — năm ISO khác năm dương lịch ở cả hai chiều |
| Đụng vào danh mục | `docs/CATEGORY_RATIONALE.md` — **lý do** của từng thay đổi, bằng chứng đo được, và các phương án đã loại bỏ. Đọc trước khi định "dọn dẹp" vùng này |
| Đụng vào mục tiêu tiết kiệm | `docs/GOAL_FEATURE.md` — **quyết định kèm lý do, và bảy cái bẫy** (4.5 và 4.6 đã đóng, còn năm). Mục 4 phải đọc trước khi sửa gì; mục **10** là đối chiếu với app thị trường, kèm bảng xếp hạng việc tiếp theo. Ba cái đáng nhớ nhất: `walletTransfer` **không có khoá ngoại**; suy chiều nạp/rút từ vị trí ví là **diễn giải lại lịch sử**; và `_collectPendingOps` dựng payload **thô** — phép quy đổi `chi → Transaction` chạy ở bước POST, đọc dừng ở đó là kết luận nhầm. Payload mục tiêu nay **22 trường** (đếm bằng máy từ chính `sync_payload_contract_test.dart` ngày 2026-09-08; các bản trước ghi 19 vì cộng `18 + priority` mà quên ba cột `auto_deposit_*` thêm hôm 2026-09-07 — **đừng cộng dồn, hãy đếm lại**). Năm tính năng mới nhất có mục riêng: **3.21** cột mốc 25/50/75% (khoá chống trùng theo khuôn `goalCycle:`, và luật phải đứng **trước** phép kiểm `isBehindSchedule`), **3.22** thứ tự ưu tiên kéo thả (schema **v19**; số cách nhau 100, `NULL` xếp cuối, và `<= 0` kéo về cũng đọc là `NULL` — lớp phòng thủ ở `uu_tien_hop_le.dart` cho hàng `0` cũ và bản client cũ, **giữ nguyên** dù backend đã hết ép `null` thành `0` (G32 đóng 2026-09-11); còn `ReorderableListView` trả `newIndex` **lệch một ô** khi kéo xuống), và **3.25** nhãn "(tự động)" (**hậu tố** ghi chú chứ không cột mới — phép đọc đòi đủ **cặp** tiền tố + hậu tố, và khoản cũ đọc là "tay", không đoán ngược), và **3.26** biểu đồ tiến độ theo thời gian (chuỗi dựng **đi lùi** từ `currentAmount` vì lịch sử không bảo đảm cộng lại bằng nó; đường kế hoạch mượn **nguyên** luật của `isBehindSchedule` nên không đẻ luật thứ hai; `startDate` null thì giấu kế hoạch chứ không giấu cả khối), và **3.27** ba con số tổng hợp (chuỗi đếm **kỳ** chứ không đếm khoản; kỳ cắt bằng `mocThuN` neo mốc gốc — **đừng** dựng bản thứ tư của luật ấy; kỳ hiện tại chưa nạp **không** phá chuỗi) |
| Đụng vào trang Phân tích | `docs/ANALYTICS_FEATURE.md` — ✅ **BA KHỐI CUỐI VÀO TỆP XUẤT xong 2026-09-17** (mục **3.31**): PDF/CSV nay có **so với kỳ trước · số liệu nhanh · top 5 khoản chi**, ba khối màn Xem trước đã hiện từ 2026-09-09 mà tệp thì không. Cả ba đã nằm sẵn trong `BaoCao` nên hạng mục gói trong **một** tệp `lib` (`domain/xuat_tep.dart`); **không đụng repository, không đổi schema, không thêm trường đồng bộ**. ⚠️ **Ba bẫy im lặng, cả ba có ca test riêng**: `_tiLe` nhận `[0,1]` rồi nhân 100 còn `phanTramSoVoi` **đã** trả thang 0–100 — dùng lại `_tiLe` ở đây là in `1250.0` cho mức tăng 12,5%; ô phần trăm của CSV phải **RỖNG** chứ không `—` (cột số Excel sắp cộng; PDF thì `—` mới đúng); và khối Số liệu nhanh trong tệp chỉ có **ba** chỉ số chứ không bốn vì "Số giao dịch" đã nằm ở khối Tổng quan. Hai ca "bỏ hẳn khối khi rỗng" **xanh ngay từ đầu** — chỉ đáng tin sau khi bản sai có chủ ý làm chúng đỏ. ⚠️ Và lượt này bắt được **lỗi glyph có từ 2026-09-09** — xem đoạn "Font PDF phải nhúng" bên dưới trong chính hàng này. ⚠️ ✅ **KỲ RỖNG THÌ TỆP THÔI IN KHỐI NGÂN SÁCH xong 2026-09-18** (mục **3.32**, đóng **G44**): người dùng chốt chiều *"tệp theo màn"* — "đã chi" của một ngân sách đếm theo kỳ của **chính nó**, nên bảng ấy trong tệp báo cáo của một kỳ rỗng nói về khoảng thời gian khác, và người cầm tờ PDF không có chỗ hỏi lại. Luật nay là vị từ thuần **`inKhoiTheoKy`** ở `domain/bao_cao_xuat.dart` mà **ba** chỗ cùng đọc (CSV · PDF · nhánh rỗng của màn Xem trước); khối *Số liệu nhanh* vốn gác bằng `!bc.rong` kèm chú thích "cùng luật với màn Xem trước" — nay chú thích ấy **thành mã**. ⚠️ **Ba chỗ dễ vấp**: ngân sách là khối **duy nhất** cần vế ấy (mọi khối khác tự rỗng theo kỳ rỗng, nên vế đứng **cạnh** `isNotEmpty` chứ không thay nó); nhánh PDF chặn tại **nguồn hàng** chứ không bọc quanh `_pdfBang` (hàm ấy tự bỏ bảng khi hết hàng, bọc ngoài là đẻ nhánh thứ hai phải giữ đồng bộ); và ca test cho PDF phải so **độ dài tệp** chứ không tìm chuỗi — PDF **nén** luồng nội dung nên `contains('NGÂN SÁCH KỲ NÀY')` xanh trên cả bản sai, bản sai có chủ ý làm ca đúng đỏ với **2 847 byte** chênh lệch. ⚠️ Lượt sửa **đính chính tiêu đề cũ của G44**: màn Xem trước **không** giấu "mọi khối" khi kỳ rỗng — đầu báo cáo, khối Dòng tiền và ba thẻ tổng vẫn hiện, và tệp in đúng ba thứ ấy, nên hai bên lệch **đúng một khối**. Không đổi schema, không thêm trường đồng bộ. — ✅ **TỔNG TÀI SẢN THEO THỜI GIAN xong 2026-09-17** (mục **3.30**, mục #5 của khảo sát lần hai): đường thứ **ba** của bộ sáu kỳ, đứng ngay sau *Dòng tiền tự do*. ⚠️ **Tên đổi khỏi "tài sản ròng"** — app không có mô hình công nợ (A8 #9 đã bỏ), nên tiền **đi vay** nằm trong ví sẽ làm "tài sản ròng" **tăng** đúng lúc người dùng mắc nợ thêm; người dùng chốt đo đúng thứ tính được. **Không nguồn stream mới**: `watchKy` vốn đã phát toàn bộ giao dịch và toàn bộ ví. ⚠️ **Ba chỗ KHÁC `dongTienCua`, phá cái nào cũng hỏng im lặng**: (1) **không** đi qua `khoanVaoThongKe` — hàm ấy loại khoản chuyển, khoản điều chỉnh số dư và khoản mở sổ, mà cả ba **đều làm đổi số dư ví thật**; chốt nằm ở **kiểu dữ liệu** (`BienDongVi` cố ý không mang `categoryId` lẫn `ghiChu`, nên không ai lọc được kể cả khi muốn); (2) tính theo **từng ví rồi mới lọc** — chuyển sang ví bị loại khỏi tổng **có** làm tài sản giảm; (3) cửa sổ biến động đóng ở **cả hai đầu** `[moc, now]` — vế sau bỏ hàng ghi **ngày tương lai**, vì `wallets.balance` cộng mọi hàng *bất kể ngày* nên khoản hẹn 10/10 đã nằm trong số dư hôm nay; **kẹp mốc một mình không cứu được**, có ca test đỏ đúng chỗ đó. ⚠️ **Giới hạn phải nói ra**: mốc **trước giao dịch đầu tiên** cho số 0 "chưa biết" — `mocThieuDuLieu` sinh câu cảnh báo, và `thayDoiTaiSan` trả **`null`** thay vì in một khoản tăng bịa (đừng thay bằng `?? 0`). Ca "khoản neo `Số dư ban đầu` ghi ngày vá" mà mục 3.25 lo **đo được là không tồn tại** trên dữ liệu thật và nằm gọn trong giới hạn ấy. 🔑 Để dành, **cố ý không làm**: server có `wallet.Create_at` và đường pull **đã trả về rồi** (`findMany` không `select`), client chỉ chưa đọc — nhưng nó vá một vách không tồn tại. Điểm cuối **bằng đúng** số dư Trang chủ (nghiệm thu máy ảo: ba chỗ cùng nói `13.590.000 đ`), nên nó là `ds.last.tong` chứ **không** phải một phép cộng ví riêng — cộng lại là bản chép tay thứ **sáu** của `viTinhVaoTong`. Trục **mượn `daiTrucDuBao`** và lượt này **siết chính hàm ấy**: `dải/2` là phép **trừ hao** làm trần rộng gấp đôi mức cần (dải 13,59M → trần 30M, đường dí sát đáy), nay là `dải/3` cộng phép kiểm đúng — vế **thứ ba** của bẫy **4.21**. Khối **ẩn khi kỳ rỗng** (khác khối Dự báo). Màn Stitch **`b0a3344924d246f9b6322fb75a3309e4`** — ⚠️ nó vẽ **đồng thời** dòng thay đổi và câu cảnh báo, một trạng thái bản thi công **không bao giờ dựng ra** (hai thứ loại trừ nhau theo thiết kế); lỗi của chính prompt, vì nó liệt kê cả hai mà không nói chúng loại trừ nhau. Chỗ lệch ấy **chỉ lộ khi xem ảnh render** — đọc văn bản HTML thì hai câu chỉ là hai chuỗi cạnh nhau. Không đổi schema, không thêm trường đồng bộ. — ✅ **LỊCH CHI TIÊU xong 2026-09-16** (mục **3.29**, mục #6 của khảo sát lần hai): lưới **lịch tháng**, ô đậm nhạt theo tổng chi của ngày, chạm một ô thì thẻ tóm tắt hiện ngay dưới lưới. **Chỉ hiện khi đơn vị là Tháng** — PocketSmith và Money Lover đều vẽ lịch chứ không vẽ dải kiểu GitHub, và một lưới 91 ô ở 411dp không đọc được; chốt đặt ở **hai lớp** (`if` trong `_than()` + guard đầu `build`), đo bằng bản sai: phá lớp 1 thì ca vẫn xanh, phá **cả hai** mới đỏ. **Không nguồn dữ liệu mới**: `soLieuNhanhCua` **đã dựng sẵn** map theo ngày bên trong nhưng không lộ ra, nên việc chính là **tách nó thành hàm dùng chung** `lichChiTieuCua` (`analytics/domain/lich_chi_tieu.dart`) và cho hàm cũ gọi lại — có ca test canh *"ngày chi nhiều nhất" của Số liệu nhanh KHỚP ô đậm nhất của lịch*. ⚠️ **Thang màu neo vào TRUNG BÌNH (`soLieu.chiMoiNgay`), không vào ngày lớn nhất** — neo vào max thì một ngày mua sắm lớn làm phẳng cả tháng, 29 ngày còn lại rơi hết về bậc nhạt nhất và lưới trông như tháng không tiêu gì. Bậc: `0` không chi · `1` ≤ 0,5× · `2` ≤ 1× · `3` ≤ 2× · `4` > 2×; `trungBinh` = 0 ra `Infinity` nên mọi ngày *có* chi về bậc cao nhất — đúng nghĩa. Ba chốt nữa: **ngày không chi có bậc 0 riêng** (gộp là "tháng không tiêu gì" trông y hệt "tháng tiêu ít"); **lưới bắt đầu thứ Hai**, ô trống `= weekday - 1`, Chủ nhật cho sáu ô — lệch một ô là **cả tháng lệch một cột** mà nhìn vẫn hợp lý; và `nhanNgayLich` **tự viết tên thứ** thay vì `DateFormat('EEEE','vi')` (hàm thuần thì không ném `LocaleDataException` làm cả cây dừng dựng) — ⚠️ Chủ nhật **không** phải "Thứ Tám", `DateTime.sunday == 7`. ⚠️ **Hai thứ chỉ bản sai và máy ảo mới lộ:** `_tieuDeKhoi` là `SizedBox(width: double.infinity)` nên **đặt trần vào `Row` làm 66/74 ca của trang đỏ cùng lúc** (bọc `Expanded`); và ca "chưa chạm thì chưa có thẻ tóm tắt" **ban đầu không canh được gì** vì nó chỉ cấm chữ "khoản", trong khi bản sai hiện thẻ cho một ngày **không chi** nên thẻ nói "Không chi" — cùng bài học G43: **ca test phải đòi KẾT QUẢ, không chỉ đòi vắng mặt thứ mình nghĩ tới**. Dựng bằng `GridView` chứ không `CustomPainter` (app chưa dùng `CustomPaint` ở đâu). Màn Stitch **`9020ff8b5c5d49c4914442dcd02fa540`** — ⚠️ lượt gọi **trả về `timeout`** mà màn vẫn được tạo (lần thứ hai xác nhận), và nó vẽ lưới **xanh lá** trong khi bản thi công dùng **đỏ** `AppColors.expense`. Không đổi schema, không thêm trường đồng bộ. — ✅ **SO CÙNG KỲ NĂM TRƯỚC xong 2026-09-16** (mục **3.28**, mục #2 của khảo sát lần hai): hàng **hai chip** ngay trên hai thẻ tổng — *"So với kỳ trước"* · *"Cùng kỳ năm trước"* — và dòng "so với …" đổi theo chip; chip trái bật sẵn nên người dùng cũ **không thấy gì đổi**. **Không có nguồn stream thứ tám**: `watchKy` vốn nạp toàn bộ giao dịch tài khoản nên kỳ năm trước chỉ là một lời gọi `tongThuChi` nữa. ⚠️ **Tuần lùi 52 kỳ, KHÔNG neo vào ngày dương lịch** — bản thiết kế đã duyệt ghi ngược, và **phép đo lật nó**: trên **3131 tuần của 60 năm**, neo thứ Hai cho kỳ so sánh chồng lấp ít nhất **1 ngày** còn `lui(ky, 52)` cho **5 ngày**, và lùi 52 kỳ trùng khít lối neo vào **thứ Năm** (ngày định danh tuần ISO) ở **cả 3131 tuần**; cũng đừng dùng "cùng số tuần ISO" vì tuần 53 không tồn tại ở phần lớn năm. Bài học: **một ca test đỏ có thể đang tố cáo bản thiết kế chứ không phải bản thi công** — đo trước, đừng sửa bên nào cho xanh. Ba chốt còn lại hỏng im lặng: khoảng tuỳ chọn **giữ nguyên độ dài** (`to' = from' + (to − from)`; dời riêng từng mốc thì kỳ bắt đầu 29/2 lệch một ngày và phần trăm so hai kỳ lệch độ dài trông vẫn hợp lý) trong khi tháng/quý/năm **được phép** khác độ dài; nhãn tuần lấy **năm ISO** chứ không `from.year` (tuần bắt đầu 30/12/2024 là tuần **1 của 2025**); và cặp *số* + *nhãn* phải lấy cùng một chỗ — `nenSoSanh` ở `domain/moc_so_sanh.dart` trả cả hai, lấy lệch là ra một câu **hoàn toàn hợp lý và hoàn toàn sai**. Cubit phải chép `_mocSoSanh` sang **mọi** state mới (cùng bẫy stream phát lại của 3.19), và **giữ** mốc khi đổi kỳ — khác `phanLoaiDangXem`. ⚠️ **Nền bằng 0 là ca THƯỜNG**: mọi tài khoản chưa đủ một năm tuổi đều rơi vào đó ở mọi kỳ, và thẻ nói *"Không có dữ liệu T9 2025"* thay vì bịa "tăng 100%". Màn Stitch **`6333b8e24aab4f92bd73b1282c56b17c`** *"Thống kê - Mốc so sánh kỳ"* (không timeout; vẫn mang `deviceType: DESKTOP` dù truyền `MOBILE`). Không đổi schema, không thêm trường đồng bộ. — ✅ **DỰ BÁO DÒNG TIỀN 30 NGÀY xong 2026-09-16** (mục **3.27**, mục #4 của khảo sát lần hai; spec `2026-09-16-du-bao-dong-tien-design.md`): khối đứng **ngay sau thẻ tổng** — ba con số (số dư → **còn tiêu được** → *nếu tiêu đúng ngân sách*), cảnh báo ví thiếu, biểu đồ **bậc thang** 31 điểm, danh sách cam kết. ⚠️ **KHÔNG có thu nhập trong dự báo** — app không lưu nó ở đâu, và suy từ lịch sử là một con số **đoán** ngồi cạnh những con số thật (cùng bẫy `tong.thu` của 3.24); người dùng chốt lối "chỉ chiếu thứ đã biết chắc". Hai tầng tách bạch: hoá đơn + trích tự động là **chắc chắn**, phần còn lại của ngân sách kỳ này là **nếu tiêu đúng kế hoạch**. Phép tính ở `analytics/domain/du_bao_dong_tien.dart`; kỳ tương lai của hoá đơn mượn **`kyKeTiepCua`** (hàm thuần **mới** ở `bill/domain/`, `_nextPeriodOf` gọi lại nó — một định nghĩa), kỳ trích mượn `mocThuN`+`quyetDinhTrich`, tầng 2 mượn `budgetPaceOf`, số dư mượn `viTinhVaoTong`. **Bốn chốt hỏng im lặng:** (1) `watchKy` nay **bảy** nguồn và nguồn ngân sách thứ bảy phải tra tại **`now`** chứ không tại mốc kỳ đang xem — dùng chung là dự báo **đúng khi xem tháng này, sai khi xem tháng khác**; (2) tầng 2 phải **trừ hoá đơn cùng danh mục** kẻo đếm đôi; (3) trích tự động **cộng ví đích** nên tác động lên tổng thường bằng 0 — trừ khi ví đích không tính vào tổng; (4) cam kết quá hạn dồn về hôm nay phải nằm ở **điểm 0** của chuỗi (`!isAfter`). Giới hạn cố ý không vá: hàng hoá đơn người dùng **tự tạo tay** cho kỳ sau bị đếm đôi (khử trùng chỉ dựa `generatedFromBillId`) — vá bằng so tên là hỏng theo chiều tệ hơn. ⚠️ Nghiệm thu máy ảo lật **hai** thứ 2633 ca test đều mù, cả hai về **trục biểu đồ** — xem bẫy **4.21**. ✅ **G42 đóng cùng ngày** (lượt soát tài liệu của chính mục này lộ ra): `analytics_repository_impl` đọc ví bằng truy vấn thẳng **không lọc `deletedAt`** — cố ý, vì bảng tra tên cần hàng đã xoá — rồi cộng `balance`, nên "số dư cuối kỳ" của khối Dòng tiền và thác nước phình đúng số dư ví người dùng đã xoá. Vế `isDeleted` nay nằm **trong `viTinhVaoTong`** (mặc định `false`), không vá ở chỗ gọi — hàm ấy vốn có tệp test sinh ra để chặn "bản chép tay thứ năm", và đây đúng là bản thứ năm. ⚠️ `bao_cao_repository_impl` và `wallet_repository_impl` **không sai** (đi qua `walletDao.getAll`, có lọc). — ✅ **TỈ LỆ TIẾT KIỆM xong 2026-09-15** (mục **3.26**, mục #1 của khảo sát thị trường lần hai ở mục **3.25**): một dòng trong thẻ "Số dư còn lại" — *"Để dành 93% thu nhập"*. ⚠️ **Mẫu số là THU NHẬP, không phải `tong.thu`** — cùng bẫy A8 #8 nhưng dễ vấp hơn vì công thức sách vở là `(thu − chi)/thu`; lấy `tong.thu` thì tháng nào vay tiền tỉ lệ lại **đẹp lên**. Phép tính thu nhập nay tách thành **`thuNhapCua()`** và `dongTienTuDo()` gọi chính nó — một định nghĩa duy nhất, có ca test canh hai chỗ không lệch. **Trả nợ tính là TIÊU** (người dùng chốt) để con số khớp "Số dư còn lại" ngay trên nó. Ba chốt hỏng im lặng: `null` khi thu nhập **không dương** thì **ẩn hẳn dòng** (chia cho mẫu số âm ra tỉ lệ **đảo dấu**; thu nhập âm xảy ra thật khi kỳ chỉ có tiền đi vay) — ca này **xanh ngay từ đầu**, phải thử bản sai `tyLe ?? 0` mới biết nó canh thật; tỉ lệ **được phép âm**; và chuỗi vay/nợ rỗng thì **bỏ qua** chứ không coi như không có vay/nợ. — ✅ **DÒNG TIỀN TỰ DO xong 2026-09-15** (A8 **#8**, mục **3.24**): một đường, sáu kỳ, đứng ngay sau khối Xu hướng. ⚠️ **Hai chữ "thu nhập" KHÔNG phải `tong.thu`** — cột ấy là **mọi** khoản `type='thu'` nên đã gồm cả tiền **đi vay** và **thu nợ**, mà tiền mượn và vốn cũ quay về đều không phải thu nhập; lấy nguyên `tong.thu − traNo` thì tháng nào vay tiền đường lại **vọt lên**, im lặng. Luật đúng: **thu nhập = tổng thu − mọi khoản tiền VÀO thuộc nhóm Vay/nợ** (`diVay`, `thuNo`, **và** `khacVao` — ô thứ ba dễ quên nhất vì nó là khoản không đoán được vai, nhưng một khoản vay/nợ tiền vào chỉ có thể là đi vay hoặc thu nợ). Đo máy ảo: thêm `Đi vay` +5.000.000 thì con số **không đổi**. `dongTienTuDo()` ở `analytics/domain/dong_tien_tu_do.dart` ghép `chuoi` và `chuoiVayNo` **theo chỉ số** và **ném `ArgumentError`** khi hai chuỗi lệch — vì thế helper `_tk` của `analytics_page_test.dart` nay dựng `chuoiVayNo` mặc định **cùng số kỳ** với `chuoi`; cho widget nuốt lỗi ấy là biến một lỗi lập trình thành một khối biến mất im lặng. Khối **luôn hiện** (người dùng chốt), đường **được phép âm** và không kẹp về 0 — kèm vạch 0 nét đứt, chấm kỳ âm đổi màu, và `cutOffY: 0` ở **cả** `belowBarData` lẫn `aboveBarData` (thiếu vế thứ hai thì vùng tô đổ xuống đáy và kỳ âm trông y hệt kỳ dương). ⚠️ Máy ảo bắt được **bẫy 4.20**: nhãn trục in **`-0`** vì `san + 3*buoc` ra `-1e-16`; sửa tại **`rutGon`** chứ không tại khối gọi — số 0 không mang dấu, cùng luật `formatCoDau`, nhưng số âm thật vẫn giữ dấu. Không đổi schema, không thêm trường đồng bộ, **không đụng repository**. 🛑 **Bảng A8 ĐÓNG từ 2026-09-16** — người dùng chốt **bỏ hẳn** hai ô cuối **#9** (biến động khoản vay) và **#11** (Sankey); đừng lên kế hoạch cho chúng nữa, và **đừng mở lại hàng đợi `CAN-LAM/`** để xin dư nợ gốc/lãi suất/kỳ hạn vì mục duy nhất cần những cột ấy đã bị bỏ (banner mục **7.1** `ANALYTICS_FEATURE.md`). Câu "còn đúng **một** ô trống là **#9**" đứng ở đây tới 2026-09-15 là ảnh chụp trước quyết định ấy. — ✅ **THÁC NƯỚC "Tiền đi đâu" xong 2026-09-15** (A8 **#10**, mục **3.23**): số dư đầu kỳ → +thu → trừ dần từng nhóm chi → số dư cuối kỳ, chín cột, mỗi nhóm chi là một khối **nổi** (`BarChartRodData.fromY/toY`). ⚠️ **Người dùng từng chốt KHÔNG LÀM mục này rồi ĐỔI Ý cùng ngày** — mọi câu "🛑 P3 không làm" ở tài liệu cũ hơn là ảnh chụp quyết định đầu. ⚠️ **Phép cân**: `đầu kỳ + thu − Σ nhóm chi` phải ra ĐÚNG `cuối kỳ`, lệch là bậc thang hở một khe **im lặng** — nên `thacNuocCua()` **không tự tính** `cuoiKy` mà nhận con số khối Dòng tiền đang hiện, và nhóm chi mượn `topVaKhac` (cùng hàm với donut). ⚠️ **Đường trung bình KHÔNG phải đường ngang**: khối chi nổi ở vùng 13,6–14,6 triệu còn mức TB là 174 nghìn nên đường ngang không cắt cột nào — mắt so **độ cao**, đường ngang so **vị trí**; nay là **vạch trên từng cột chi** (`ranhVuotTrungBinh`, ngưỡng **nửa đồng** cho đuôi lẻ `double`). ⚠️ **`BarChartData` không có `clipData`** — bẫy 4.17 là của `LineChartData`; ở đây `minY`/`maxY` quét cả `tu` lẫn `den`. Ba chốt: `dongTien == null` thì **ẩn cả khối** (chốt đặt ở **hai lớp**, bản sai phải phá cả hai mới làm test đỏ); **trục từ 0, giữ đúng tỷ lệ thật** (người dùng chốt sau khi xem ảnh: chi chỉ bằng 7% thu nên khối mỏng là **đúng**, bóp méo trục là vẽ sai sự thật); và **ô nhãn trục hoành ≤ 32dp** — chín cột trên 411dp chỉ được ~36dp mỗi cột, bản đầu để 46dp làm chín nhãn dính thành chuỗi không đọc được, và `flutter test` **không bắt được** vì `find.text` so `data` chứ không so thứ vẽ ra (bẫy 4.4). — ✅ **A8 #4 VÀ #5 xong 2026-09-15** (mục **3.22**): hai biểu đồ cột **"Cho vay & Thu nợ"** và **"Đi vay & Trả nợ"**, mỗi kỳ một **cặp cột chồng nhau** (cột sau rộng+mờ, cột trước hẹp+đậm đè lên chính giữa; `barsSpace` **âm** `-(rộng1+rộng2)/2`, và **thứ tự trong `barRods` là thứ tự vẽ**). ⚠️ **Câu "A8 #4/#5 bị chặn bởi mô hình dữ liệu" là QUÁ CHẶT** — hai biểu đồ này chỉ vẽ *dòng tiền*, không cần dư nợ gốc/lãi/kỳ hạn; riêng **#9** thì vẫn chặn thật. ⚠️ **Tên danh mục là QUAN HỆ nợ, chiều tiền mới là VAI**: màn Thêm giao dịch có ô **"Chiều tiền"** cho danh mục Vay/nợ, nên `Cho vay`+tiền vào = **thu nợ**, `Đi vay`+tiền ra = **trả nợ** — hai danh mục mặc định đủ ghi bốn vai (tài khoản thật **chỉ có** `Cho vay` và `Đi vay`; `Trả nợ`/`Thu nợ` đã xoá mềm trên server). Bản đầu hiểu ngược (coi là "tên nói dối" → `khac`) làm hai cột *Thu nợ* và *Trả nợ* **không bao giờ có số**, im lặng — chỉ chạy thật trên máy ảo mới thấy. `vaiVayNoCua` ở `domain/vai_vay_no.dart` là định nghĩa duy nhất; **không bỏ dấu** khi so tên (quy tắc 7); "thuộc nhóm Vay/nợ" mượn `phanLoaiCua` cùng với donut; **màu theo chiều tiền** nên khối 1 cột sau đỏ còn khối 2 cột sau xanh; khoản không đoán được vai có **khối thứ ba riêng**, chỉ hiện khi có. — ✅ **BỐN KHỐI MƯỢN TỪ TRANG BÁO CÁO xong 2026-09-15** (P2, mục **3.21**): trang nay có **dòng tiền · số liệu nhanh · phân bổ theo ví · top 5 khoản chi**, thứ tự khối chép đúng trang Báo cáo. Bốn phép tính là **hàm thuần dùng chung** ở `bao_cao_xuat.dart` (`soLieuNhanhCua`, `phanBoTheoVi`, `topKhoanChi`, `dongTienCua`) — ⚠️ **đừng gọi `dungBaoCao`** từ trang Phân tích: nó tính thừa cả chuỗi biểu đồ và bảng danh mục, mà stream này phát lại sau **mọi** chu kỳ đồng bộ. Repository nhận **nguồn thứ tư là ví** (tên ví + tổng số dư qua `viTinhVaoTong`). ⚠️ **Cột số tiền của hai khối bảng phải là `Expanded`, không phải `Flexible`** — cả hai cùng `flex: 1` nên chia đôi chỗ trống như nhau, nhưng `Flexible` để con giữ bề rộng tự nhiên và phần thừa rơi về **cuối hàng**, nên mép phải răng cưa (đo được **26,5px**); `Expanded` cho con chiếm trọn suất thì `alignment: centerRight` mới có mốc, và cột trái **không mất chỗ nào**. Bẫy **4.19**, có ca test so `getRect(...).right`. ✅ **Trang Xem trước báo cáo cũng đã sửa 2026-09-15** (G40 đóng) — nó mang **sáu** chỗ cùng khuôn chứ không phải hai, và lệch **93px**, gấp ba trang Phân tích; đo lại sau khi sửa: 0px. ⚠️ **Ca test cho lỗi này KHÔNG viết được ở khổ 411dp**: font "Ahem" rộng gấp đôi nên mọi chuỗi đều tràn suất và `FittedBox` co lại lấp đầy — ba trong sáu ca xanh ngay từ đầu trong khi máy ảo đo được 93px. Phải chạy ở khổ **gấp đôi** (822dp), và phép đo phải bám **hộp** (`find.byWidgetPredicate` cho `FittedBox` canh phải) chứ không bám chuỗi. Bốn bẫy nữa: khối dòng tiền **luôn** kèm câu "Suy ngược từ số dư hiện tại của các ví" (có test canh đúng câu ấy); `dongTienCua` nhận **toàn bộ** giao dịch chứ không phải phần đã cắt theo kỳ (đưa danh sách đã cắt vào là đầu kỳ bằng cuối kỳ, im lặng); **số 0 không mang dấu** — `CurrencyFormatter.formatCoDau` là định nghĩa duy nhất, `-0 đ` ở ví chỉ có thu là lỗi máy ảo bắt được; và test của trang **phải** gọi `initializeDateFormatting`, thiếu nó thì khối Top 5 ném `LocaleDataException` và **cả cây dừng dựng** — mọi ca đỏ với dáng vẻ "khối không hiện". — ✅ **PHẠM VI THỜI GIAN xong 2026-09-15** (P1, mục **3.20**; spec `2026-09-15-pham-vi-thoi-gian-trang-phan-tich-design.md`): trang nay xem được theo **tuần · tháng · quý · năm · khoảng tuỳ chọn**, không còn khoá cứng `(nam, thang)`. **`Ky` là định nghĩa duy nhất** (`analytics/domain/pham_vi_ky.dart`) — một `DonViKy` cộng biên `[from, to)`; nó thay `(nam, thang)` ở `ThongKeKy`, `watchKy`, `AnalyticsLoading`, `chonKy` và mọi nhãn. Ba dạng nhãn cho ba chỗ, **không** phải thừa: `nhan` (danh sách bộ chọn), `nhanNgan` (ô header vốn đã tràn 53px một lần; ⚠️ **nhãn quý là `Q3 2026` chứ không `Quý 3 2026`** — sửa 2026-09-15 sau khi người dùng báo: dạng đầy đủ làm `"Quý này (Quý 3 2026)"` dài hơn `"Tháng này (T9 2026)"` **đúng một ký tự** và máy ảo cắt thành `"Quý này (Quý 3 20…"`, mất cả năm. **Không nhãn nào được dài hơn nhãn tháng** — đó là chuỗi duy nhất đã chứng minh vừa trên máy thật, và có ca test canh bất đẳng thức ấy ở **tầng thuần** chứ không đo bề rộng, vì font test rộng gấp đôi nên ở 411dp chuỗi nào cũng cụt. `Q3` không phải quy ước thứ hai — trục biểu đồ đã dùng `Q3/26`), `nhanTruc` (trục biểu đồ); cộng `nhanOChon` (nếp "Tháng này (T9 2026)" chỉ khi kỳ **chứa hôm nay**) và `nhanKyTruoc` (câu "so với …" — bỏ năm khi cùng năm, giữ năm khi kỳ trước sang năm khác). Bộ chọn là **bottom sheet hai tầng** (`presentation/widgets/chon_pham_vi_sheet.dart`), không phải chip trên header. **Sáu bẫy, cả sáu hỏng im lặng** (cái thứ sáu thêm 2026-09-16 sau khi đóng **G43**, và nó là lỗi **của chính lát P1 này**)**:** (1) ⚠️ **ngân sách chỉ gắn khi đơn vị là Tháng** — `BudgetView.spent` đếm theo kỳ của *chính ngân sách ấy*, vẽ nó cạnh số liệu một tuần là so hai kỳ khác nhau, dòng phải rơi về "% tổng chi"; (2) ⚠️ **nhãn trục của sáu kỳ liên tiếp phải đôi một khác nhau** — sáu quý trải qua một năm rưỡi nên `Q3` một mình xuất hiện **hai lần**, nhãn quý là `Q3/26`, có ca quét cả bốn đơn vị canh (cùng họ G39); (3) nhãn trục tuần là **ngày thứ Hai** chứ không `T38` vì `T` đang là tiền tố của tháng; (4) **thoát bộ chọn ngày thì giữ nguyên kỳ đang xem**, khác `khoangCuaPhamVi` của trang Báo cáo (⚠️ hàm ấy **đã bỏ** 2026-09-18 — trang Báo cáo nay dùng chính bộ chọn này, nên luật ấy áp cho **cả hai** trang); (5) **`lui` lùi theo đơn vị lịch**, không trừ số ngày; (6) ⚠️ **khoảng khởi tạo của `showDateRangePicker` phải KẸP vào `[firstDate, lastDate]`** — bản đầu truyền thẳng kỳ đang xem trong khi `lastDate` là **hôm nay**, nên từ *Tuần này · Tháng này · Quý này · Năm nay* (trạng thái **mặc định**) là assertion nổ, và vì nó nằm trong hàm `async` **không ai bắt** nên nút "Tuỳ chọn" chỉ **không làm gì**: không toast, không màn đỏ, chỉ một dòng logcat. Phép kẹp nay là hàm thuần `khoangKhoiTaoBoChonNgay` (trả `null` khi kỳ không giao với dải), bốn ca widget chạm **thật** vào nút và đòi `DateRangePickerDialog` hiện ra — chỉ `takeException() isNull` thì vẫn xanh với một nút chết. **G43**, bẫy **4.22**. ⚠️ Và một bẫy chỉ máy ảo thấy: **sheet phải cao CỐ ĐỊNH** — mỗi đơn vị có số kỳ khác nhau (12 tuần · 8 quý · 5 năm) nên để nó co theo số dòng là hàng chip trượt xuống dưới ngón tay và cú chạm kế rơi vào lớp phủ. `bienTuan` tách khỏi `tuanTruoc` ở `core/notification/tuan_iso.dart` để bộ chọn và thông báo Tổng kết tuần dùng chung một phép. ⚠️ **P1 KHÔNG gỡ chặn cho Tổng kết tuần** — thông báo ấy xong từ 2026-09-09 (mục 5d `NOTIFICATION_FEATURE.md`), điều kiện của nó đóng bằng phạm vi tuỳ chỉnh của trang Xuất báo cáo; ghi chú 2026-09-08 trong spec của nó là ảnh chụp một ngày trước, và khối "✅ Đã đủ" nằm ngay dưới. Không đổi schema, không thêm trường đồng bộ; trang Xuất báo cáo chỉ đổi một dòng `export` vì `khoangKyTruoc` chuyển sang `pham_vi_ky.dart`. — ✅ **A8 #3, #7 xong 2026-09-14** (mục **3.19**, **bản thi công lần hai**): khối **"Cơ cấu theo danh mục"** có **ba chip** *Chi · Thu · Vay-nợ* chọn nhóm (nhóm **Chi mở sẵn**, chip chỉ hiện cho nhóm **có phát sinh**, thứ tự cố định theo `kCategoryClassifies` chứ không theo số tiền), và khối Xu hướng có **hàng chip chọn NHIỀU** — tập rỗng là hai đường Thu/Chi, tối đa **`kToiDaDuongXuHuong` = 5** đường, đủ trần thì chip chưa bật bị **khoá nhìn thấy được** (`onSelected` null) còn chip đang bật luôn tắt được; chốt thật ở cubit, khoá ở widget chỉ để nhìn thấy. ⚠️ Mục **#2** (tròn theo phân loại: mức gốc ba lát + drill-down + dropdown chọn một) **đã làm rồi BỎ** cùng ngày — người dùng chốt; mọi tài liệu nói "mức gốc", "ba lát", "dropdown" là tả bản đầu, kể cả spec (spec có banner đầu tệp ghi **bốn** chỗ khác, chỗ thứ tư là màn Stitch). `theoPhanLoai()` **giữ lại** vì nó là nguồn duy nhất cho biết nhóm nào có phát sinh. **Luật phân loại có một định nghĩa duy nhất** ở `analytics/domain/phan_loai_dong_tien.dart`: `classify` của danh mục, **rơi về `transaction.type`** khi không tra được — hai thứ ấy **khác nhau**, khoản *Trả nợ* mang `type='chi'` nhưng `classify='vay_no'`. ⚠️ Hệ quả **cố ý**: nhóm "Chi" **KHÔNG bằng** "Tổng chi" ở thẻ đầu trang (phần chi gắn danh mục vay/nợ đã sang nhóm Vay/nợ; ba nhóm phải rời nhau thì tỷ trọng mới có nghĩa) — đừng "sửa" cho khớp. `chiTheoDanhMuc()` **giữ nguyên không đụng**: nó gom theo *chiều tiền* cho trang Xuất báo cáo, hàm mới gom theo *phân loại danh mục*. Danh sách danh mục cuối trang **đi theo** chip của donut nên hai khối luôn nói cùng một con số. Bốn bẫy im lặng ghi ở 3.19, đáng sợ nhất là **stream phát lại sau mỗi chu kỳ đồng bộ** — quên chép hai lựa chọn sang state mới thì donut nhảy về nhóm Chi và mọi đường xu hướng biến mất trong khi người dùng đang xem. ⚠️ **Câu "bốn mục A8 còn lại (#4, #5, #8, #9) bị chặn bởi mô hình dữ liệu" viết ngày 2026-09-14 nay CHỈ CÒN ĐÚNG VỚI #9** — sửa 2026-09-15. Đúng là không đầu nào có **bảng khoản vay** (đếm bằng máy 2026-09-14: client 9 bảng Drift, backend 13 model Prisma — không dư nợ gốc, không lãi suất, không kỳ hạn), nhưng chỉ **#9** (biến động khoản vay) mới cần những thứ ấy. **#4 và #5 đã làm xong** ngày 2026-09-15 vì chúng chỉ vẽ *dòng tiền*; và **#8** (dòng tiền tự do) cũng **không bị chặn** — ✅ **đã làm xong cùng ngày**, mục **3.24**; ⚠️ nhưng công thức **không** phải `Σ thu − Σ traNo` như câu này từng ghi, vì `tong.thu` đã gồm cả tiền đi vay và thu nợ. Cái thật sự thiếu hồi ấy là chỗ lưu **vai**, và vai suy ra được từ *tên danh mục + chiều tiền*. Bài học: một mục bị xếp "chặn bởi mô hình dữ liệu" thì phải hỏi *chặn vì thiếu con số nào*, chứ đừng gộp cả nhóm theo cái tên "vay/nợ". `suggestDebtDirection()` thì vẫn chỉ đoán theo **tên** nên chỉ dùng được lúc nhập liệu. *(Ảnh chụp 2026-09-14, nay đã lỗi thời: "🛑 #10 người dùng chốt không làm" — họ **đổi ý cùng ngày** và #10 đã xong.)* *(Ảnh chụp 2026-09-14–15, nay lỗi thời: "Còn lại **#11** (Sankey) làm được với thu/chi nhưng chưa làm" — người dùng chốt **bỏ hẳn** #11 cùng #9 ngày 2026-09-16.)* Màn Stitch: **`c8567243…`** *"Thống kê - Cơ cấu danh mục & Xu hướng 6 tháng"* cho thân trang, **`83993fc9…`** *"Thống kê - Chọn phạm vi thời gian"* (2026-09-15) cho bộ chọn phạm vi, **`6e9007f7653749a893c88e3de535afa5`** *"Thống kê - Biểu đồ Cho vay & Đi vay"* (đo được 2026-09-15) cho hai biểu đồ vay/nợ, **`afe1c3fdee43464c90ddadc508eaa599`** *"Thống kê - 4 Thẻ Dòng Tiền & Kế Toán"* (đo được 2026-09-15) cho bốn khối của P2, **`52450ac549df42aea9f31d5ee1347ceb`** *"Thống kê - Biểu đồ thác nước Tiền đi đâu"* và **`212befc8f9a24d25ab027c7302e2e122`** *"Thống kê - Biểu đồ Dòng tiền tự do"* (cả hai đo được 2026-09-15) — ⚠️ màn thứ tư mang `deviceType: DESKTOP` dù lượt gọi truyền `MOBILE`, nhưng thân trang dựng trong `max-w-[430px]` căn giữa nên bố cục vẫn là một cột điện thoại; và lượt gọi ấy **trả về `timeout`** rồi mãi lượt kiểm thứ ba (chừng mười lăm phút) mới thấy màn — timeout **không phải** thất bại, đừng gọi lại — ⚠️ màn thứ ba vẽ thêm **hai thẻ tổng** (*số kỳ hạn*, *số kỳ còn lại*, *% tiến độ*) mà bản thi công **cố ý bỏ**, vì cả ba con số ấy đòi **dư nợ gốc và kỳ hạn** — đúng thứ mô hình dữ liệu không có, và vẽ bằng số suy đoán là bịa một con số người dùng sẽ tin; ngược lại khối thứ ba *"Vay/nợ chưa xếp được vai"* thì Stitch không có mà bản thi công thêm vào. **Hai** màn cũ `c2a2b615…` (tả A8 #2 đã bỏ) và `a228fa69…` đều lỗi thời nhưng vẫn còn trong dự án. — lát **2a** (số thật thay số cứng) và **2b** (khối "Xu hướng 6 tháng") xong 2026-09-08; **2c‑1** và **2c‑1b** xong 2026-09-09 (trang Xuất báo cáo đọc số thật, mở màn **Xem trước báo cáo** nay có **mười khối**: dòng tiền, so kỳ trước, biểu đồ, số liệu nhanh, thu/chi theo danh mục, ngân sách, phân bổ theo ví, top 5 khoản chi — mục **3.15** ghi khảo sát Money Lover/MISA/Copilot/PocketSmith đứng sau các khối ấy). **2c‑2** cũng xong: nút "Tải xuống" sinh tệp **PDF hoặc CSV** thật và **lưu thẳng vào thư mục Tải về** qua `MediaStore` (kênh `flowmoney/luu_tep`, mã Kotlin trong `MainActivity` — **chỗ mã gốc đầu tiên do dự án tự viết**); Android ≤ 9 lùi về sheet chia sẻ. ⚠️ Câu *"mảng Phân tích đến đây là xong"* từng đứng ở đây **không còn đúng** — người dùng chốt làm tiếp ngày 2026-09-14. Font PDF phải **nhúng** (`assets/fonts/Roboto-*.ttf`): font mặc định của gói `pdf` không có dấu tiếng Việt và **mất dấu im lặng**; có test canh bằng cách cấm chuỗi "Helvetica" xuất hiện trong tệp. ⚠️ **Nhúng đúng font vẫn CHƯA ĐỦ** (đo 2026-09-17): bản Roboto ấy **không có** khối Mũi tên (U+2190…) lẫn khối Hình học (U+25A0…), nên `→ ↑ ↓ ▲ ▼` bị gói `pdf` **bỏ đi im lặng** — dòng dòng tiền của mọi PDF app từng xuất đã mất mũi tên suốt từ 2026-09-09 tới khi sửa, và bản thiết kế đầu của mục 3.31 suýt lặp lại y hệt với `▲`/`▼`. Roboto **có** `»` `›` `•` `±` `−` `·` `—`. Phép canh: một ca test quét **chuỗi hằng của chính `xuat_tep.dart`** (bỏ dòng chú thích, vì chú thích mang `⚠️`) rồi đòi mọi ký tự ngoài ASCII có trong `charToGlyphIndexMap` của **cả hai** tệp Roboto — `TtfParser` là API công khai của gói `pdf`. Chữ của *người dùng* thì không chặn trước được. CSV thì bắt buộc **BOM + dòng `sep=;` + số nguyên thô mang dấu** — mục **3.17** và **3.18**. Bốn thứ dễ vấp: `'transfer'` **không** phải thu/chi (và bị loại khỏi **cả** danh sách của báo cáo); "% ngân sách" mượn `watchBudgets(now: mốc)` và **phải lọc `isExpired`**; font của bộ test rộng gấp đôi ngoài đời nên tiêu đề/nhãn phải co được (bẫy 4.4); và **theme của app ép mọi `ElevatedButton` rộng vô hạn** — nút trần trong `Row` làm trắng cả trang mà không một dòng log nào, nên widget test phải dựng bằng `AppTheme.lightTheme` (bẫy **4.11**). Riêng **dòng tiền** (số dư đầu/cuối kỳ) là số **suy ngược** từ số dư hiện tại: nó **biến mất khi lọc theo một ví** và lệch khi có ví tạo giữa kỳ — mục **3.16**, đừng "sửa" thành luôn hiện |
| Đụng vào **biểu đồ** (bất kỳ đâu trong app) | Thư viện là **`fl_chart`, ghim cứng `1.2.0`** (không `^`) — mục **3.11** `docs/ANALYTICS_FEATURE.md` nói vì sao chọn nó, vì sao ghim, và ba lý do **không** phải lý do chọn. Chỗ chọn ấy nay đã dùng cho **chín** khối biểu đồ của app — đếm lại bằng máy 2026-09-17 sau khi thêm "Tổng tài sản theo thời gian" (mốc **tám** là của 2026-09-16, sau "Dự báo 30 ngày tới" — **đừng cộng dồn, hãy đếm lại**). ⚠️ **Lịch chi tiêu** (mục 3.29, cùng ngày) **KHÔNG** nằm trong số ấy: nó là `GridView`, không phải `fl_chart` — đừng cộng nó vào con số này. Và đây là **chín khối người dùng thấy** chứ không phải chín lời gọi (mã chỉ có **8**: `_KhoiVayNo` là một class dựng **hai** lần). Bảy khối ở trang Phân tích: "Xu hướng …" (tiêu đề **đổi theo đơn vị** từ 2026-09-15 — 6 tuần · 6 tháng · 6 quý · 6 năm, định nghĩa ở `tieuDeXuHuong`), **"Tiền đi đâu"** (thác nước, `BarChart` với `fromY`/`toY`), **"Dòng tiền tự do …"** (tiêu đề cũng đổi theo đơn vị, `tieuDeDongTienTuDo`; đây là khối **duy nhất** có `minY` âm), "Cho vay & Thu nợ", "Đi vay & Trả nợ", **"Dự báo 30 ngày tới"** (2026-09-16, bậc thang — chỗ **đầu tiên** dùng `isStepLineChart`) và **"Tổng tài sản … gần đây"** (2026-09-17, mục 3.30 — đường thứ **ba** của bộ sáu kỳ; ⚠️ câu "khối **duy nhất** có trục co theo dữ liệu" đứng ở đây tới 2026-09-16 nay **sai**: khối này cũng co trục, và nó **mượn lại `daiTrucDuBao`** chứ không đẻ luật thứ hai). Cộng "Thu chi trong kỳ" ở màn Xem trước báo cáo và "Tiến độ theo thời gian" ở trang chi tiết mục tiêu. ⚠️ Câu "**cả hai** khối biểu đồ" đứng ở đây tới 2026-09-15 là ảnh chụp của ngày 2026-09-09 — **đừng cộng dồn, hãy đếm lại**. Phép tính phải nằm ở tầng domain thuần và được test ở đó; tầng vẽ **không test được** — vị trí tooltip, màu, nét, và đường cong vọt dưới 0 chỉ kiểm được bằng mắt trên máy ảo 411dp (bẫy **4.9**, đã vấp thật). Hai bẫy thêm ngày 2026-09-09, **cả hai đều chỉ lộ trên máy thật**: **4.17** `clipData` mặc định là `FlClipData.none()` nên điểm ngoài dải vẫn được **vẽ** và tràn khỏi thẻ (đặt `FlClipData.all()` cho mọi biểu đồ), và **4.18** fl_chart vẽ nhãn trục ở **cả hai biên** cộng thêm mốc theo `interval` nên hai nhãn cuối chồng nhau. ⚠️ Bẫy **4.21** thêm 2026-09-16 gộp hai luật về TRỤC, cả hai do máy ảo bắt: **cột thì trục từ 0** (so độ cao — thác nước, vay/nợ), **đường số dư thì CO theo dữ liệu** (so hình dạng — dự báo; trục từ 0 cho ra một đường phẳng khi cam kết chỉ bằng 2,8% số dư); và khi co trục thì **bước phải TRÒN** (họ 1·2·2,5·5 × 10^k, sàn là bội của bước) — bước lẻ làm biên và mốc cuối lệch vài phần tỉ, hai nhãn cùng nội dung in đè nhau. Phép ấy ở `daiTrucDuBao`, tầng thuần, có test |
| Đụng vào **loại ví** | `lib/features/wallet/domain/wallet_type.dart` là **nguồn duy nhất** — nhãn, khoá cục bộ, khoá đẩy lên. Chỉ **ba** loại chọn được (`cash`, `bank`, `saving`), và tạo được **nhiều** ví `saving` — chốt tạm "một ví Tiết kiệm" (theo `uq_wallet_saving_active` của server) gỡ ngày 2026-09-11 sau khi `database/12` bỏ index (G30; ⚠️ máy chủ nào chưa áp `database/12` vẫn từ chối ví Tiết kiệm thứ hai (`UNIQUE_VIOLATION`, vĩnh viễn)); `banking` **đọc được nhưng không tạo được** vì nó chỉ có nghĩa khi đi kèm `Id_bank_casso`, thứ chỉ luồng liên kết ngân hàng của server tạo ra. ⚠️ PostgreSQL có `chk_wallet_type` chỉ nhận `Cash \| Bank \| Saving \| Banking`: giá trị lạ lọt lên là bản ghi **kẹt hàng đợi đẩy vĩnh viễn, im lặng** — đó là lỗi đã có thật với `ewallet`/`debt` của giao diện cũ, đóng ngày 2026-09-09 (migration **v20**). Đo lại đầy đủ 2026-09-10: bảng có **18** ràng buộc, đủ cả bốn `chk_wallet_*`. ⚠️ Nhưng CHECK **không** thay được việc kiểm độ rộng cột: `Type` là `varchar(7)`, vừa khít `'Banking'` — xem hàng dưới để biết `Status` đã vấp đúng chỗ đó. Enum là **Dart thuần** để `sync_payload_normalizer.dart` dùng được; biểu tượng nằm ở `presentation/widgets/wallet_type_icon.dart`. Ba chỗ ánh xạ phải khớp nhau: `tuKhoa`, `walletForPush`, và migration v20. **Trạng thái** ví (`active`/`inactive`, tính năng **lưu trữ ví**) nằm ở tệp riêng `domain/wallet_status.dart`, cùng khuôn — và ✅ **đi qua đồng bộ hai chiều từ 2026-09-14** (G28), xem hàng dưới |
| Đụng vào **quản lý ví** (khác *loại* ví ở trên) | ✅ **CỜ "CHO PHÉP ÂM" từ 2026-09-17** (G27 đóng, schema **v23**, cột `wallets.allow_negative`): ví đánh dấu cho phép âm — thẻ tín dụng, ví theo dõi nợ — **không sinh cảnh báo số dư nào cả** (cả `walletNegative` lẫn `walletLowBalance`, chốt đặt ở **đầu** vòng lặp `_walletCandidates` trước cả hai nhánh), và màn Quản lý ví thôi tô đỏ **cả biểu tượng lẫn số tiền**. ⚠️ **CỘT CỤC BỘ, không đi qua đồng bộ** — payload ví vẫn **13 trường**; có **test quét `lib/` thứ bảy** canh cờ không lọt vào `sync_engine`/`sync_payload_normalizer`/hợp đồng payload. Chỗ bám là một **cờ** chứ không phải loại ví: khôi phục chuỗi `'debt'` là tái hiện đúng sự cố `chk_wallet_type` từng làm ví kẹt hàng đợi đẩy vĩnh viễn. ⚠️ **Hai bẫy chỉ bản sai và máy ảo mới lộ**: ca "cờ cũng tắt cảnh báo sắp cạn" phải dựng ví số dư **DƯƠNG** dưới ngưỡng (ví âm không bao giờ đi qua nhánh ấy, nên ca dùng số âm **không canh gì** — họ G43); và bản đầu chỉ đổi màu **biểu tượng** còn con số `-100.000 đ` vẫn đỏ — màu ở đó là `AppColors.error`, tức màu **cảnh báo** chứ không phải quy ước dấu. — ✅ **SÁU LỖ HỔNG ĐÓNG 2026-09-18** (**G45**, mở và đóng cùng ngày; không đến từ báo lỗi mà từ một lượt soát đối chiếu **từng ràng buộc PostgreSQL với chốt tương ứng phía client**). Nặng nhất: **ô nhập số dư không giới hạn chữ số** mà `Balance` là `numeric(15,2)` — tràn cho SQLSTATE `22003`, `sync.service.js` **không có nhánh** cho mã ấy nên trả `DB_ERROR`, và `_permanentCodes` là **danh sách trắng** nên ví bị **gửi lại ở mọi chu kỳ đồng bộ**, im lặng (cùng vòng lặp G31/G14, khác cột). Nay có `GioiHanSoChuSo` + `kSoChuSoToiDaSoTien` ở `core/utils/gioi_han_do_dai.dart`. Năm cái còn lại: `softDelete` **không chặn ví gắn hoá đơn** (server để `fk_bill_wallet` là `ON DELETE RESTRICT`; đo dữ liệu thật: **một ví xoá được ngay** trong 26 hoá đơn đang gắn ví) → nay là ràng buộc **thứ tư**; phép đếm giao dịch **bỏ sót khoản chuyển đến** vì `getByWallet` chỉ lọc `walletId` → nay `softDelete` hỏi qua **`demGiaoDichLienQuan`** (đếm cả hai vai; `getByWallet` **giữ nguyên nghĩa cũ** vì hai ca test mục tiêu dựa vào nó); thông báo *"điều chuyển số dư về 0đ trước khi xóa"* **dẫn tới ngõ cụt** từ sau G37 (mọi cách đưa số dư về 0 đều sinh thêm giao dịch) → nay chỉ sang **lưu trữ**; `currency` là cột CHECK **duy nhất** chưa được `walletForPush` che → nay có `tienTeGuiLen`, **nâng chữ thường** chứ không gộp mù về `VND`; và thiếu chốt cấm đặt ví **đã lưu trữ** làm mặc định → `_kiemViMacDinhConDung`, **chỉ canh đường người dùng chủ động** vì nhánh kéo về ghi thẳng qua DAO. ⚠️ **Hai lỗi NỮA do chính lượt vá sinh ra, cả hai chỉ máy ảo thấy**: giới hạn 13 chữ số biến một trạng thái vô nghĩa thành **hợp lệ đạt tới được** nên `9.999.999.999.999` tràn bố cục **70px** (sửa bằng widget `OSoDuVi`), rồi widget ấy đọc `controller.text` **một lần** nên cỡ chữ không co khi gõ và số dài **cuộn khuất mất chữ số đầu** — bố cục vẫn lành nhờ `Flexible` nên **không sọc vàng nào để thấy** (sửa bằng `ValueListenableBuilder`). ⚠️ **Một bản sai lật kết luận**: thứ chặn tràn là **`Flexible`** bọc `IntrinsicWidth`, **không phải** bậc thang cỡ chữ — ép cỡ chữ cố định mà hai ca bố cục **vẫn xanh**; `coChuSoDu` lo việc khác (giữ số đọc được hết), nên ca test của nó đo **thẳng giá trị trả về**. Cùng họ G43. Không đổi schema, payload ví vẫn 13 trường. — **Bảy** bất biến, tất cả đều hỏng **im lặng** nếu phá — bốn cái đầu đóng ngày 2026-09-09, hai cái tiếp đến cùng tính năng lưu trữ ví 2026-09-10, cái cuối là ràng buộc trùng tên của server phát hiện cùng ngày (dòng này từng ghi "sáu", rồi "tám" khi còn luật một ví Tiết kiệm — gỡ 2026-09-11, G30); mục 14 `docs/PROJECT_CONTEXT.md`. **Một ví mặc định mỗi tài khoản**: chốt ở `WalletLocalDataSourceImpl` chứ không ở repository, vì cả đường thêm lẫn đường sửa đều qua datasource; và `getDefault` **phải chịu được hai hàng** vì trạng thái ấy đến từ server qua `upsertAll` (⚠️ câu "không unique index nào chặn ở cả hai đầu" ghi ở đây trước 2026-09-10 là **sai**: server **có** `uq_wallet_default_active`, cùng ba partial unique index khác trên `wallet` — chúng chỉ không hiện ở `pg_constraint`; SQLite thì vẫn không có gì chặn). **Ràng buộc server mà client phải chặn trước** (2026-09-10, `wallet/domain/rang_buoc_vi.dart` — định nghĩa duy nhất, chốt ở `WalletLocalDataSourceImpl._kiemRangBuocServer`): **tên ví duy nhất trong tài khoản** (`uq_wallet_account_name_active`, so bằng `normalizeCategoryName` — siết hơn server, có chủ ý). Từng có luật thứ hai, **một ví Tiết kiệm mỗi tài khoản** (`uq_wallet_saving_active`), chỉ tồn tại ở SQL — backend bỏ ở `database/12`, client gỡ chốt ngày 2026-09-11 (G30, `DA-XONG/WALLET_SAVING_INDEX.md`). Vi phạm luật trùng tên là 23505 → `WALLET_NAME_DUPLICATE` (từ `7675b35`; nhận diện bằng chuỗi nên có thể rơi về `UNIQUE_VIOLATION` — chưa thử khi chạy, CAN-LAM 18 §2.7; mã nào cũng vĩnh viễn) → ví **kẹt hàng đợi đẩy vĩnh viễn, im lặng**, và mọi giao dịch trong ví ấy vỡ FK rồi thử lại mãi. Ví đã xoá mềm không giữ chỗ; ví **lưu trữ thì vẫn giữ**. **Thứ tự hiển thị** có một định nghĩa duy nhất ở `WalletDao._thuTuHienThi` (ví mặc định → `lower(name)`); đổi nó là đổi **mọi** đường đọc danh sách ví trong app — đếm bằng máy 2026-09-10: **7** chỗ gọi `getAll`, **3** chỗ `watchAll`, **9** chỗ `getActive` (con số "14 chỗ gọi" ghi ở đây trước 2026-09-10 đúng cho tới khi có hai phép đọc). Bảng `wallets` **không có `createdAt`** nên tên là mốc ổn định duy nhất hiện có. **Cờ mặc định chọn sẵn ví** khi ghi giao dịch qua `transaction/domain/vi_chon_san.dart`, và ví đích phải là ví **khác** ví nguồn chứ không phải chỉ số 1. **Lưu trữ ví là ĐÓNG BĂNG, không phải xoá** (2026-09-10): có **hai** phép đọc danh sách — `getActive` cho **bộ chọn ví**, `getAll`/`watchAll` cho những chỗ phải thấy ví lưu trữ (màn Quản lý ví, bảng tra tên ví, đường đồng bộ). Chọn nhầm **không gây lỗi nào**, nên `test/features/wallet/wallet_picker_sources_test.dart` quét cả `lib/` và bắt mọi chỗ gọi phải được phân loại **tay** kèm lý do. Cố ý **không** có `watchActive`. Hai chốt chặn khi lưu trữ — không lưu trữ ví **mặc định**, không lưu trữ **ví hoạt động cuối cùng** — nằm ở `WalletLocalDataSourceImpl.setArchived`, và **màn Sửa ví phải đi qua đó** chứ không `copyWith(status:)`: màn ấy gọi thẳng `updateWallet`, nên ghi trạng thái kiểu kia là đi vòng qua cả hai chốt. Phép "ví nào cộng vào tổng tài sản" có một định nghĩa duy nhất ở `wallet/domain/vi_tinh_vao_tong.dart` — nó lọc **cả** `includeInTotal` lẫn ví lưu trữ, và ba chỗ cộng tổng (trang chủ, báo cáo, repository) đều phải đi qua nó. ⚠️ **SỐ DƯ VÍ SUY TỪ SỔ GIAO DỊCH từ 2026-09-13** (G37 đóng, spec `2026-09-13-so-du-vi-suy-tu-so-giao-dich-design.md`): `wallets.balance` thôi là dữ liệu gốc, nó là **cache của một công thức** — `Σ thu − Σ chi − Σ transfer đi + Σ transfer đến`, định nghĩa duy nhất ở `TransactionDao.tongTheoVi`. Điểm neo là **một giao dịch** "Số dư ban đầu" (`wallet/domain/so_du_mo_so.dart`), id suy **tất định** từ `walletId` nên hai máy cùng vá thì ra một hàng; nó mang **cặp** dấu hiệu như khoản điều chỉnh và bị `khoanVaoThongKe()` loại. **`SoDuViService` là nơi DUY NHẤT ghi `balance`** — test quét `lib/` thứ **sáu** cấm mọi nơi khác gọi `updateBalance`; 50 chỗ *đọc* `.balance` thì không đổi gì. Nhánh kéo về **thôi đọc** cột ấy (`Value.absent()`) và tính lại sau mỗi lần pull; nhánh **đẩy giữ nguyên**, payload ví vẫn 12 trường. ⚠️ **Ba cái bẫy, cả ba chỉ lộ ra khi chạy thật** — neo tính bằng `balance − Σ sổ` nên **thời điểm** đặt nó quyết định đúng sai: đặt SAU khi ghi sổ thì neo hấp thụ luôn giao dịch vừa ghi (53 ca test đỏ); để `tinhLaiSoDu` tự đặt neo thì ví số dư 0 nhận tiền sẽ sinh một khoản **chi** triệt tiêu đúng khoản ấy; và đặt neo cho ví **vừa kéo về** (mang `balance = 0` vì nhánh pull thôi đọc) là neo âm bằng cả tổng sổ — **số dư 0 nghĩa là "chưa biết", không phải "ví rỗng"**. Ví `banking` **bị loại khỏi mọi phép tính lại** vì server tự ghi số dư ví ngân hàng từ SePay. **Đối soát số dư** (2026-09-10): ô số dư ở màn Sửa ví **không còn ghi đè** — nó sinh một khoản bù qua `wallet/data/services/dieu_chinh_so_du_service.dart`, đi đường `TransactionRepository.addTransaction` để phép cộng trừ **và phép hoàn lại khi xoá** đều dùng lại `_applyBalances`. Khoản bù là `thu`/`chi` chứ **không** `transfer`, vì `_applyBalances` cố ý bỏ qua khoản chuyển thiếu ví đích. Nhận dạng bằng **cặp** điều kiện — không danh mục **và** tiền tố `Điều chỉnh số dư` — vì ghi chú sửa được còn danh mục rỗng là thứ giao diện thêm giao dịch không tạo ra được; ⚠️ riêng chân danh mục **không đủ** (17 hàng trên server đang trống danh mục thật). Luật loại khỏi thống kê có **một** định nghĩa ở `analytics/domain/khoan_vao_thong_ke.dart`, thay cho **năm** bản chép tay của câu `!= 'transfer'`. Ba chốt: chênh lệch 0 thì không ghi (`chk_transaction_nonzero_amount`), ngưỡng **nửa đồng** cho đuôi lẻ của `double`, và **ví lưu trữ thì từ chối**. **`include_in_total` đi cả hai chiều** từ 2026-09-09. ✅ **`status` ĐI QUA ĐỒNG BỘ HAI CHIỀU từ 2026-09-14** (G28 đóng, schema **v22**, payload ví **13 trường**; spec `2026-09-14-g28-luu-tru-vi-qua-dong-bo-design.md`): lưu trữ ví trên máy A nay có hiệu lực trên máy B. **Ba mảnh khớp nhau, thiếu mảnh nào cũng vô hiệu cả ba** — payload mang `status`, `walletForPush` dịch sang `'Active'`/`'Inactive'` qua `WalletStatus.khoaGuiLen`, nhánh kéo về đọc ngược lại về **chữ thường**. Bốn điều dễ vấp: mở **một** chiều thôi là hỏng im lặng (chỉ đẩy → server giữ `'Active'` rồi ví tự bỏ lưu trữ sau một chu kỳ pull; chỉ kéo về → thao tác lưu trữ không bao giờ rời máy); giá trị lạ **không** được giữ nguyên mà gộp về `'Active'`, vì `chk_wallet_status` chỉ nhận đúng hai chuỗi và một giá trị lạ lọt lên là ví **kẹt hàng đợi đẩy vĩnh viễn** (đúng bài học `ewallet`/`debt` ở cột `Type`, v20); nhánh kéo về dùng `Value.absent()` khi server im lặng, dù cột server hiện `NOT NULL` — đó là lớp phòng thủ trước bản backend không trả khoá; và **migration v22 đánh dấu ví lưu trữ cũ để đẩy lại** là bắt buộc, vì chúng đang ở `synced` nên không ai gửi lại chúng trong khi server giữ `'Active'`, và bước cứu ấy chỉ chạy **một lần trong đời** mỗi máy. *Lịch sử, giữ vì nó giải thích hình dạng hôm nay:* cột này từng **cục bộ** (2026-09-10) do lược đồ PostgreSQL tự mâu thuẫn — `chk_wallet_status` cho phép `'Inactive'` nhưng kiểu cột là **`varchar(7)`** mà chuỗi ấy dài **8 ký tự**, nên đẩy lên là ví kẹt hàng đợi, vấp thật trên máy ảo; CSDL dev áp `database/7` tối cùng ngày và cột nới lên `varchar(20)` (đo lại 2026-09-14: `varchar(20)`, `NOT NULL`, `DEFAULT 'Active'`). ⚠️ **Máy chủ nào chưa áp `database/7` vẫn từ chối `'Inactive'`** — ví lưu trữ kẹt hàng đợi đẩy vĩnh viễn, im lặng, và client không chặn trước được. Tài liệu xin nới cột: `docs/superpowers/backend/DA-XONG/WALLET_STATUS_COLUMN_WIDTH.md` |
| Đụng vào **hiển thị số tiền** | `lib/core/utils/currency_formatter.dart` là **nơi DUY NHẤT** được dựng `NumberFormat` — có test quét cả `lib/` để cấm chỗ khác. Quy tắc: **chấm** ngăn nghìn, **phẩy** cho thập phân, ký hiệu **`đ`** (không phải `₫`). `format()` **làm tròn về đồng chẵn**; muốn phần lẻ thì `formatCoLe()`; ô nhập liệu dùng `formatSoThoi()` (không kèm ký hiệu); chỗ cần dấu **theo chiều tiền** thì `formatCoDau(soTien, thu: …)` — giống `formatIncome`/`formatExpense` nhưng **số 0 không mang dấu**, vì `-0 đ` ở một ví chỉ có thu đọc như một con số âm bằng không (thêm 2026-09-15, máy ảo bắt được). ⚠️ Đừng cho `format()` hiện phần lẻ: mọi số **tính ra** (thiếu hụt, trung bình ngày) đều có đuôi lẻ và màn hình sẽ đầy `7.927.272,73 đ`. CSV xuất báo cáo **vẫn là số nguyên thô**, không đi qua đây |
| Đụng vào **ô nhập TIỀN** (bất kỳ đâu) | ⚠️ **Mọi ô nhập tiền phải có trần số chữ số** — `GioiHanSoChuSo(kSoChuSoToiDaSoTien)` ở `core/utils/gioi_han_do_dai.dart`, và **test quét `lib/` thứ tám** (`o_nhap_tien_co_tran_test.dart`) canh việc ấy. Lý do: **tám** cột tiền trên PostgreSQL đều là `numeric(15,2)`, tức nhiều nhất **13 chữ số** phần nguyên. Tràn cho SQLSTATE `22003`, mà `sync.service.js` **không có nhánh** cho mã ấy nên trả `DB_ERROR`; `_permanentCodes` của `SyncEngine` là **danh sách trắng** nên bản ghi bị **gửi lại ở mọi chu kỳ đồng bộ**, im lặng — cùng vòng lặp G31/G14. Thiếu trần **không gây lỗi nào lúc gõ**: ô vẫn nhận, màn vẫn lưu, SQLite vẫn ghi; chỉ hàng đợi đẩy hỏng, ở nơi không ai nhìn. ⚠️ **Màn Thêm giao dịch là ngoại lệ**: bàn phím ở đó **tự vẽ** nên `inputFormatters` không với tới — phép gõ đi qua hàm thuần **`themPhimSoTien`** (`transaction/domain/ban_phim_so_tien.dart`), và phím `000` **cắt bớt cho vừa trần** thay vì bỏ cả cụm. ⚠️ **Và mỗi lần đặt trần thì phải thử luôn bố cục với giá trị lớn nhất** — ngày 2026-09-18 việc này vấp **ba lần trong một ngày**: trần biến giá trị lớn nhất thành **hợp lệ đạt tới được**, rồi bố cục chưa từng được thử với nó mới lộ ra. Hai kiểu hỏng khác nhau: `TextField` thì **tràn** (có sọc vàng, chặn bằng `Flexible` — **không** phải bằng cỡ chữ, đo bằng bản sai), còn `Text` thì **ngắt dòng im lặng** (không sọc vàng, `takeException()` trả `null`, chặn bằng `FittedBox` + `maxLines: 1` và ca test phải đo **chiều cao thật**). `FittedBox` dùng được cho `Text` mà **không** dùng được cho `TextField`: nó đo con ở ràng buộc vô hạn, mà `TextField` không có bề rộng tự nhiên ở đó |
| Đụng vào **ô nhập tên** (ví, mục tiêu, hoá đơn, danh mục) | `lib/core/utils/gioi_han_do_dai.dart` là định nghĩa duy nhất: `DoRongCot` giữ độ rộng cột đo trên PostgreSQL (tên ví, mục tiêu, hoá đơn **100**; tên danh mục **200** — đo 2026-09-10), còn `GioiHanDoRong` là bộ lọc ô nhập đếm theo **code point**. ⚠️ Đừng thay bằng `maxLength`: nó đếm grapheme nên để lọt chữ tách dấu và emoji, và vẽ bộ đếm "0/100" mà Stitch không có. Thêm một ô tên mới mà quên bộ lọc thì không gì báo — tên dài hơn vỡ `P2000` và bản ghi **không bao giờ lên server**: từ `7675b35` backend trả `CONSTRAINT_VIOLATION` nên client xếp nó lỗi vĩnh viễn, trước đó là `DB_ERROR` và bị gửi lại **mãi** (G31, đóng 2026-09-11). Bảy ô hiện có, mỗi ô một widget test canh |
| Đụng vào **kênh thời gian thực** (Socket.io) | Spec `docs/superpowers/specs/2026-09-09-socket-io-realtime-channel-design.md`, và khối "✅ Socket.io phía client" ở mục 14 `docs/PROJECT_CONTEXT.md`. **Ba thứ dễ vấp nhất:** payload là **hộp đen**, client chỉ đọc **tên sự kiện** — vì `bank_transaction.incoming` được backend phát từ **hai** chỗ với **hai** hình dạng, và trường `type` mang **hai** nghĩa (⚠️ **một ngoại lệ** từ 2026-09-12: `account.force_logout` **có** đọc payload, vì nó chỉ phát từ **một** hàm `core/socket.js:175-191`; nó đi luồng **riêng** `RealtimeChannel.buocDangXuat` chứ không qua `events`, đúng để cam kết này không bị nới lỏng theo) (tài liệu: `DA-XONG/SOCKET_BANK_EVENT_PAYLOAD.md`; từ `7675b35` mỗi giao dịch còn phát **hai lần** — CAN-LAM 18 §2.5, ✅ hết từ `cbbeeb4` 2026-09-12: worker chỉ còn publish EventBus, một chỗ phát duy nhất ở `notification.service.js` — cam kết hộp đen **giữ nguyên**); `io.io()` **cache `Manager` theo `scheme://host:port`** và dùng lại options của lần dựng đầu nên phải `enableForceNew()`, nếu không token mới bị bỏ qua **im lặng**, và cũng vì thế cơ chế nối lại của thư viện bị **tắt** để mỗi lần thử đọc lại token; `AppConstants.baseUrl` có hậu tố **`/api`** còn socket thì không — dùng `socketBaseUrlFrom()`. Kênh nghe **cả `onConnectivityChanged`** để mạng về là nối ngay chứ không nằm chờ hết giãn cách (đo được 26 giây chết trên máy ảo trước khi vá). Chỉ **`realtime_socket.dart`** được import `socket_io_client`. ✅ Từ 2026-09-12 (gộp `main` @ `cbbeeb4`) kênh **nối được**: đo trên máy ảo, sau 51 lần bị từ chối liên tiếp thì `[RealtimeChannel] Đã nối (idaccount=11)` đúng lúc backend nạp mã mới, và mạng về thì nối lại sau 2 giây (trước đó, 2026-09-11, bắt tay từ chối mọi tài khoản — CAN-LAM 17 A — và client coi mỗi lần bị từ chối là một lần nối hỏng nên hẹn nối lại mãi). Backend phát `sync.completed` sau mỗi `/sync/push` tới phòng `account_<id>`, và ✅ **client nghe từ tối 2026-09-12** (G34 đóng): `RealtimeEvent.dongBoXong`, đánh thức `syncNow()`, **im lặng** — `loiNhan` nay là `String?` và **`null` là định nghĩa duy nhất của "không toast"**; đừng thêm toast cho nó: máy vừa đẩy cũng nhận lại sự kiện của mình và payload là hộp đen nên toast sẽ nói sai trên đúng máy vừa ghi. Đo máy ảo hai máy cùng tài khoản: máy kia kéo về **cùng giây** |
| Đụng vào **xác thực**, **xoá tài khoản** hoặc **cưỡng chế đăng xuất** | Spec `docs/superpowers/specs/2026-09-10-cuong-che-dang-xuat-va-cho-xoa-design.md` (**đã duyệt 2026-09-11**; Phần 2–3 trừ §5.1 **đã xong 2026-09-11** — **G33**, đóng cùng ngày, `docs/CLIENT_APP_KNOWN_GAPS.md`; mục 6 (việc cho backend) — CAN-LAM 19 — ✅ **backend làm xong, gộp `cbbeeb4` 2026-09-12**: `getProfile` trả `countdown`, `pendingDeleteCancelled` gỡ; **client đọc cùng ngày** — `_dongBoTrangThai` ghi số và mốc nhận kể cả khi trạng thái khớp (4 ca test mới, một ca cũ đổi tên); §3.8 **đã sửa 2026-09-11** (chỉ 400/401 do server **trả lời** `/auth/refresh` là phiên chết, giữ token khi mất mạng/5xx; nhiều 401 cùng lúc chờ chung một lượt làm mới); **Phần 1 (§3.1–§3.7) và §5.1 đã làm 2026-09-12** — ba nguồn (socket, 401 request thường, 401 `/auth/refresh`) đổ về **một** event `TaiKhoanBiBuocDangXuat` của `AuthBloc`, kiểu chung ở `lib/core/auth/buoc_dang_xuat.dart`, và hộp thoại có lý do ở màn Đăng nhập. **Bốn chốt dễ phá nhất:** chỉ đọc `code` khi `statusCode == 401` (body 400 của repo cũng mang `code`, đọc nhầm là hiện hộp thoại "bị vô hiệu hoá" cho một lỗi nhập liệu); màn Đăng nhập phải đọc state ở **cả** `initState` lẫn `BlocListener` vì router dựng trang **sau** khi bloc emit; dọn SQLite chỉ khi `daXoa` **và** nguồn khác `lamMoi` (§3.6b); và ⚠️ **HAI chốt quanh cuộc đua với `_onAuthCheckRequested`** — `verifySession()` xếp chính cái 401 ấy là phiên chết nên đường ấy **cũng** đăng xuất, song song: (a) chốt state ở đầu handler phải nhận cả `AuthUnauthenticated` (lời từ chối tới muộn), và (b) mọi lượt phát `AuthUnauthenticated` của các đường *phiên chết* phải đi qua `_phatChuaDangNhap(emit)` để mang theo lý do đã nhớ ở `_thongBaoBuocDangXuat` — `logout()` là lời gọi **mạng** nên đường trơn thường về **sau** và đè mất lý do. Thiếu **một trong hai** là app đăng xuất **không kèm hộp thoại**: người dùng bị đá ra mà không biết vì sao. Cả hai vấp thật trên máy ảo 2026-09-12 trong khi 63 ca test đều xanh — loại lỗi thứ ba của mục "Ba loại lỗi `flutter test` KHÔNG bắt được". Nhánh HTTP 401 nay **đã kiểm đầu-cuối trên máy thật** (hộp thoại hiện đúng, SQLite giữ nguyên dữ liệu); nhánh socket và nhánh làm mới **đã kiểm đầu-cuối chiều 2026-09-12** qua API admin khoá tài khoản thử — hộp thoại hiện trong ~100 ms từ `account.force_logout`, SQLite giữ nguyên; `/auth/refresh` 200 khi hợp lệ, 401 + `code` khi khoá; ⚠️ `UPDATE` thẳng CSDL **không** kiểm được nhánh socket vì sự kiện chỉ phát từ mã backend — xem khối ✅ đầu §7.3). Tài khoản chờ xoá nay **dùng tiếp được app** trong 30 ngày thay vì bị đăng xuất ngay: thẻ ở Trang chủ và thẻ "Vùng nguy hiểm" ở Cài đặt hiện số ngày khi máy biết số, và nút huỷ yêu cầu. ⚠️ Từng có **hai** đặc tả nói ngược nhau: bản 2026-08-17 (`docs/superpowers/auth/`, gitignore) cho *đăng nhập lại là tự khôi phục*, còn `docs/progress/Client-app.md` mục 12 cho *dùng tiếp 30 ngày, huỷ bằng nút* — **backend chạy theo mục 12** (`pendingDeleteCancelled` luôn `false`), và **trang Xoá tài khoản của client nay khớp mục 12** (sửa 2026-09-11, G33 đóng). Trên nhánh đã gộp `main` @ `cc65f4f` (đo 2026-09-11): body 401 của HTTP **mang** `code`/`idaccount`/`reason_inactive` ở cấp gốc và lỗi lược đồ ở `authenticate` thành 503 (phần HTTP của CAN-LAM 13); `accountRejection` khi ấy không bao giờ trả `null` nên bắt tay socket và `/auth/refresh` từ chối **mọi** tài khoản (CAN-LAM 17 A), lời từ chối không mang mã (18 §2.1). ✅ **Gộp `cbbeeb4` ngày 2026-09-12 sửa cả ba**: hàm trả `null` khi hợp lệ (chạy thật 4 ca), hai chỗ gọi xử lý `SCHEMA_ERROR` **trước** (socket: lỗi không kèm mã; refresh: 503) rồi đọc `rejection.data`; bắt tay **đã nối trên máy ảo**, nhánh làm mới **đã đo** chiều cùng ngày (200 hợp lệ; 401 + `ACCOUNT_INACTIVE` + `reason_inactive` ở cấp gốc khi khoá). Interceptor tự làm mới cũng đã chạy thật (token 1 phút hết hạn → xoay token, không đăng xuất). Ngoại lệ `lamMoi` ở §3.6b **vẫn giữ**, nhưng ⚠️ **lý lẽ của nó đã bị phép đo ngày 2026-09-13 lật, và kết luận cũ ghi ở đây là SAI từ bản `7779999`**. Câu cũ: *xoá qua admin thu hồi refresh token nên `/auth/refresh` trả 401 không mã trước khi kiểm tài khoản — đường `lamMoi` + `daXoa` không xảy ra được* (G36; xin backend CAN-LAM 20 §2.7). ✅ **G36 ĐÓNG 2026-09-13** — đo đầu-cuối **ba** ca qua API admin trên mã `7779999`: `Active` → **200**; admin khoá → **401 + `ACCOUNT_INACTIVE`** kèm `idaccount` và `reason_inactive` ở cấp gốc; admin **xoá mềm** → **401 + `ACCOUNT_DELETED`** kèm `idaccount`, **dù 4/4 refresh token đã bị thu hồi**. Tức đường `lamMoi` **CÓ** mang `daXoa`; ngoại lệ §3.6b nay chặn một ca **thật** chứ không chỉ che ca lỗi lược đồ, và điều kiện "bỏ ngoại lệ khi đo được nhánh làm mới chạy đúng" mà spec đặt ra **đã thoả** — ✅ người dùng chốt **GIỮ** ngày 2026-09-13 (SQLite là bản duy nhất trên máy; nếu xoá là đúng thì `purgeDataForOtherAccounts` vẫn dọn khi tài khoản khác đăng nhập — chỉ hoãn, không bỏ) — đừng "dọn dẹp" điều kiện `nguon != lamMoi`. Client **không phải đổi mã** (`tuBody401` đã đọc đúng, ca `lamMoi` + `ACCOUNT_DELETED` có test sẵn). ⚠️ Hai bẫy khi đo lại: `PATCH /admin/updatestatus/:id` nhận **`iduser`** chứ không phải `idaccount`, và đặt `Inactive` **bắt buộc** kèm `reason_inactive` — thiếu là 400 và phép đo hỏng **im lặng** (trạng thái vẫn `Active` nên refresh trả 200, trông như backend sai). Chưa đo trên máy ảo vì `JWT_USER_ACCESS_EXPIRES=7d`, ép hết hạn phải sửa `.env` của `src/Backend` — cần cho phép đích danh |
| Đụng vào **trích tiền tự động** hoặc **tự động thanh toán hoá đơn** | Mục **3.12 và 3.13** `docs/GOAL_FEATURE.md` trước đã; hoá đơn thì mục **6.5** `docs/bill/BILL_DOCUMENTATION.md` và spec `docs/superpowers/specs/2026-09-06-bill-auto-pay-design.md`. Đây là **hai** chỗ trong app tự chuyển tiền khi người dùng vắng mặt, nên phần lớn thiết kế là về việc *dừng đúng lúc*. Hoá đơn khác mục tiêu ở chỗ **không có "lần chạy cuối"**: mỗi kỳ là một hàng, cờ đã trả là chốt chống trả hai lần; bộ chạy đi qua `payBill` với `occurredAt = dueDate`, trả bù trần 3 kỳ, và ✅ cột `auto_pay` **đi qua đồng bộ từ 2026-09-13** (bước 12). Rủi ro *hai máy cùng bật là hai khoản chi* nay đóng bằng **ba** mảnh khớp nhau, thiếu mảnh nào cũng vô hiệu cả ba: `auto_pay` đồng bộ + `chanTraHaiLan` ở `upsertTransaction` phía server (`7779999`) + `BillPaymentConflictResolver` phía client. Đo trên hai máy ảo: server còn **đúng một** khoản chi sống, hoá đơn `Payed`, **một** kỳ kế tiếp. Bốn chốt quanh resolver nằm ở hàng "Đụng vào hoá đơn" bên trên và mục **6.8** `BILL_DOCUMENTATION.md` — đọc trước khi sửa nó. Ba thứ dễ hỏng nhất: mốc chạy chỉ đặt **khi bật công tắc** (không phải ngày tạo mục tiêu); ví thiếu tiền thì **giữ nguyên mốc** để kỳ ấy tự thử lại; và `null` mang **hai nghĩa khác nhau** trong `updateGoal` — `cycleTakeMoney` là *xoá*, `icon`/`colour` là *giữ nguyên*. Lịch nhắc đi chung bộ đặt lịch với hoá đơn, **bắt buộc** — xem `NOTIFICATION_FEATURE.md` |

> ⚠️ **Tài liệu là ảnh chụp, không phải nguồn sự thật.** Luôn đối chiếu với mã nguồn thật trước khi kết luận. Phiên 2026-09-02 có nhiều kết luận sai vì tin vào tài liệu/trí nhớ thay vì mở file ra đọc.

---

## Quy tắc chí mạng

1. **Chỉ sửa `src/Client-app`.** Không đụng `src/Backend` trừ khi được cho phép rõ ràng trong chính yêu cầu đó. Cần backend làm gì thì **viết tài liệu** vào `docs/superpowers/backend/`.
   - ⚠️ **Một câu duyệt chung KHÔNG gỡ được quy tắc này** (vấp ngày 2026-09-10). Tôi trình ba lối cho một việc, một lối là "tôi sửa cả hai đầu", người dùng đáp *"hãy làm theo đề xuất của bạn"* — và tôi đã đổi `schema.prisma` rồi chạy `prisma migrate deploy` lên PostgreSQL thật. Họ không hề có ý gỡ quy tắc. Phép duyệt phải **gọi tên chính việc bị cấm**; "ok", "làm đi", "theo đề xuất của bạn" thì không. Và **đừng gộp** một lối vi phạm quy tắc chung với việc được phép vào cùng một câu hỏi — tách ra, để một chữ duyệt không thể bị hiểu thành duyệt cả hai.
   - **Viết tài liệu LÀ phần việc hoàn tất**, không kèm lời mời tự làm. Tài liệu phải tự đủ để người nhận làm theo mà không hỏi lại: đo được gì, chạy gì, áp theo quy ước nào của repo, và kiểm lại bằng câu truy vấn nào. ⚠️ Repo có **hai** quy ước: `prisma/migrations/` + `migrate deploy` + `generate` (CSDL dev chỉ có 3 dòng `_prisma_migrations`), và `src/Backend/database/N_*.sql` áp tay — `docs/Rule_Project/Rule_project.md` §3.2 nay chọn cái sau, nhưng chưa có sổ ghi môi trường nào đã áp tệp nào (tệp 5 tới 13, đo 2026-09-12 — CAN-LAM 18 §2.4, backend vẫn chưa làm). Viết tài liệu xin migration thì hỏi rõ theo quy ước nào; xem mục 4.2 `DA-XONG/DEV_DB_MIGRATIONS_7_11.md`. Khuôn đầy đủ: `DA-XONG/WALLET_STATUS_COLUMN_WIDTH.md`.
   - ✅ **CSDL dev trên máy này đã trở về đúng lược đồ chuẩn** (2026-09-10). Sự việc trên từng để lại `wallet."Status"` ở `varchar(16)` và một dòng thừa trong `_prisma_migrations`; người dùng yêu cầu **đích danh** chạy hai lệnh hoàn tác, và đo lại sau đó: cột về `varchar(7)`, lịch sử migration còn 3 dòng, bốn `chk_wallet_*` còn đủ, số hàng không đổi. Nên từ đây **đẩy `'Inactive'` lên lại vỡ như cũ** — G28 vẫn mở, đúng như tài liệu xin. Diễn biến ở mục **3b** của tài liệu vừa dẫn. ⚠️ Sau khi gộp `main` cùng ngày: "lược đồ chuẩn" của **mã nguồn** backend nay là `VarChar(20)` (`7523c8c`, tệp `database/7_…sql`); Tệp ấy **đã được áp** lên CSDL dev tối 2026-09-10, cùng 8–11, vì người dùng yêu cầu **đích danh** *"đối với csdl thì bạn hãy chạy để áp dụng csdl mới"* — cột nay `varchar(20)` (banner đầu `DA-XONG/DEV_DB_MIGRATIONS_7_11.md`). Tệp `database/12` áp ngày 2026-09-11, cũng theo một câu gọi đúng việc — *"hãy chạy để cập nhật database mới nhất"* — kèm `prisma generate` (khối "Áp `database/12`" mục 14 `PROJECT_CONTEXT.md`). Tệp `database/13` áp ngày 2026-09-12 theo câu *"hãy cập nhật csdl lên bảng mới nhất"* — một giao tác `pg`, `budget."Threshold_Warning_Percent"` mất `DEFAULT 0`, không cần `prisma generate` vì `schema.prisma` không đổi (khối "Gộp `main` @ `cbbeeb4`…" mục 14). Không có một câu cho phép gọi tên đúng việc như thế thì client **vẫn không** được tự áp migration nào.
   - ⚠️ **Tài liệu do backend tạo cũng là vùng chỉ đọc** (người dùng đặt 2026-09-10): `docs/Rule_Project/*`, `docs/progress/Backend.md`, `docs/progress/Client-app.md`. Chỗ sai của chúng → viết hướng dẫn trong `docs/superpowers/backend/CAN-LAM/`, **không** sửa thẳng, kể cả một dòng ghi chú. Tài liệu do client tạo thì được sửa và phải khớp hệ thống. Không chắc ai tạo một tệp thì `git log --follow --format='%an' -- <tệp>` trước. Các dòng client **từng** chèn vào ba tệp ấy liệt kê ở mục 8 `DA-XONG/RULE_PROJECT_DOC_DRIFT.md`.
   - `docs/` gốc **không** bị `.gitignore` chặn, nhưng một số thư mục con thì có (`docs/category/`, `docs/bill/`, `docs/deploy_Cloud/`, `docs/superpowers/plans/`…). Tạo tài liệu ở chỗ mới thì kiểm trước bằng `git check-ignore -v <path>`, nếu không nó biến mất âm thầm.

2. **`idaccount` CHỈ đến từ phiên đăng nhập.** Không bao giờ suy ra từ dữ liệu trong SQLite, không bao giờ mặc định về `1` — đó là tài khoản **admin thật**, không phải giá trị "chưa biết".

3. **Pull dùng `insertAllOnConflictUpdate`, KHÔNG dùng `insertOrReplace`.** `insertOrReplace` thay **cả hàng**, mọi cột không gán bị đưa về mặc định — từng xoá sạch cấu trúc nhóm danh mục sau mỗi lần pull.

4. **Tên trường sai thì im lặng, không báo lỗi.** Payload đi qua ba nơi định nghĩa độc lập (client dựng tay → `SyncPayloadNormalizer` → `mapEntityFields` phía backend). Thêm trường mới cho sync thì **phải** cập nhật `sync_payload_contract_test.dart` cùng lúc.
   - ⚠️ **`provider`, `bank_tran_id`, `status`, `images` KHÔNG nằm trong hợp đồng đồng bộ theo chiều nào cả** — payload đẩy giao dịch có **13 trường** (đếm lại 2026-09-12 sau khi `idbill` vào; mốc **12** là của 2026-09-08 sau khi `idgoal` vào ngày 2026-09-07, và **11** trước nữa) và nhánh kéo về cũng không đọc chúng. Cột tồn tại ở cả hai đầu nên nhìn qua rất dễ tưởng là có. ✅ Ví **từng** có một cột như thế nhưng nay hết: `wallet.status` (lưu trữ ví) đi qua đồng bộ **hai chiều** từ 2026-09-14 (G28 đóng) — payload đẩy ví nay **13 trường** (đếm bằng máy cùng ngày từ chính `sync_payload_contract_test.dart`; mốc **12** đúng tới trước bản ấy). Nó từng không đi chiều nào vì cột trên PostgreSQL là `varchar(7)` mà giá trị cần gửi dài 8 ký tự; CSDL dev nới lên `varchar(20)` tối 2026-09-10 và client mở lại ngày 2026-09-14. ⚠️ **Máy chủ nào chưa áp `database/7` vẫn từ chối `'Inactive'`** — ví lưu trữ kẹt hàng đợi đẩy vĩnh viễn, im lặng; client không chặn trước được.
   - ✅ **Điều kiện chặn đã gỡ ngày 2026-09-07.** `uq_transaction_external` nay là `UNIQUE ("Idaccount", "Provider", "Bank_tran_id")` — đo thẳng trên CSDL, không phải đọc tài liệu. Trước đó ràng buộc là **toàn cục** và chỉ trơ vì client gửi lên toàn NULL, nên thêm hai trường ấy vào payload là tự tạo vòng lặp đẩy vô hạn giữa hai tài khoản khác nhau. Nay `docs/progress/Client-app.md` mục 6.4 làm theo được. Vẫn phải **cập nhật `sync_payload_contract_test.dart` cùng lúc**, và nhớ backend đã đổi nhà cung cấp **Casso → SePay** (client còn cột `bank_casso_id` và giá trị provider `'Casso'`).

5. **Không xoá vật lý dữ liệu người dùng** — dùng soft delete (`delete_at` / `isDeleted`).
   - ⚠️ Điều này áp dụng cho **cả PostgreSQL**, kể cả với bản ghi thử của chính mình. Một hàng đã từng đồng bộ thì client vẫn giữ bản sao; xoá cứng ở server khiến client đẩy lên và nhận `Record not found` **ở mọi chu kỳ**, kẹt vòng lặp vô hạn và kéo chậm cả hàng đợi. Đã vấp ngày 2026-09-04. Muốn dọn thì đặt `delete_at`, hoặc xoá qua giao diện để cờ xoá đi đúng đường đồng bộ.
   - ⚠️ Backend **không nhất quán tên cột**, ít nhất ba kiểu: `category` dùng `Delete_at`, `transaction` dùng `Deleted_at`, và cột ngày của giao dịch là `DateTransaction` (không gạch dưới) chứ không phải `Date_transaction`. Đừng suy tên từ bảng này sang bảng kia — mở `schema.prisma` ra đọc. Truy vấn sai tên cột ở PostgreSQL thì báo lỗi ngay, nhưng viết sai trong payload đồng bộ thì **im lặng** (quy tắc 4).

6. **`.gitignore` có `test/`** (dòng **81**, đo lại bằng `git check-ignore -v` ngày 2026-09-15 — mốc 78 là của 2026-09-10, trước nữa là 77; số dòng trôi mỗi lần có người thêm dòng phía trên, và dòng ngay trước nó là `Test/`) → mọi file test tạo mới đều bị git bỏ qua **âm thầm**. Nhớ `git add -f`, nếu không công sức viết test sẽ biến mất khỏi repo.
   - ⚠️ **`git add src/Client-app/test` (cả thư mục) thất bại kể cả khi mọi file bên trong đã được theo dõi.** Git từ chối nguyên lệnh và không stage gì cả. Phải liệt kê từng đường dẫn kèm `-f`.
   - ⚠️ **Công cụ Grep tôn trọng `.gitignore` nên KHÔNG nhìn thấy thư mục `test/`.** Dò xem còn ai gọi một hàm sắp xoá thì phải dùng `grep` qua shell, nếu không sẽ thấy thiếu file và xoá nhầm.

7. **Tên danh mục là duy nhất trong phạm vi một tài khoản** — **không** tính `classify`, **không** tính nhóm cha, và tính **cả danh mục mặc định** (chúng dùng chung không gian tên với danh mục người dùng). Hàng đã xoá mềm không giữ chỗ. Hai tài khoản khác nhau thì được trùng tên.
   - Phép so tên có **một định nghĩa duy nhất**: `normalizeCategoryName()` ở `lib/core/category/category_name.dart` — NFC → chữ thường → trim → gom khoảng trắng. **Đừng tự viết lại biến thể khác**; trước đây mỗi nơi một kiểu và chúng đã lệch nhau.
   - ⚠️ **Cùng file đó còn `removeVietnameseTones()` — TUYỆT ĐỐI không dùng nó cho quy tắc trùng tên.** Bỏ dấu là phép so *mất thông tin*: "đá" với "da", "sắn" với "săn" thành một. Nó chỉ dành cho **gợi ý**, nơi đoán sai chỉ tốn một cú chạm để sửa. Siết quy tắc trùng tên bằng nó sẽ từ chối những cặp tên hợp lệ mà người dùng phân biệt được bằng mắt.
   - Thi hành ở `CategoryManagementRepositoryImpl._hasDuplicateName()`. **Đừng** thay nó bằng `getCategoryRows` — hàm đó lọc theo `classify` và khử trùng lặp theo tên, tức loại đi đúng những hàng cần đối chiếu.
   - Phép kiểm tra **chỉ chạy khi tên thật sự đổi**, để người dùng còn sửa được danh mục cũ do bản client trước tạo ra. Đây là chủ ý, không phải lỗ hổng.
   - **Nơi thi hành:** client và Admin-web kiểm trước khi ghi; đường `/sync/push` **vẫn không kiểm trước** — nó chỉ dịch lỗi của CSDL thành mã `CATEGORY_NAME_DUPLICATE`. Nhưng từ 2026-09-07 hai unique index của PostgreSQL thi hành **đúng cùng một quy tắc** với client: `uq_category_owner_name` khoá `(Create_by, tên chuẩn hoá NFC)` — **không** có `Classify` — và `uq_category_default_name` cho hàng mặc định, cả hai đều `WHERE "Delete_at" IS NULL`. Trigger chéo "người dùng không được trùng tên với mặc định" đã DROP, đúng như mô hình bản sao cần.
   - ✅ Vế "**chặt hơn**" của CSDL **đã đóng ngày 2026-09-07**: hai index mới đều có `WHERE "Delete_at" IS NULL`, nên xoá rồi tạo lại một danh mục cùng tên không còn trả 23505. Đường kích hoạt tự lặp ở mỗi lần mở app đã đóng từ trước, 2026-09-05. Vẫn đọc **G16** trong `docs/CLIENT_APP_KNOWN_GAPS.md` trước khi đụng vào `DefaultCategorySeeder`, `PersonalDefaultCategories` hay `getNamesInUse` — phép phân loại lỗi vĩnh viễn phía client vẫn cần, chỉ là đường kích hoạt đã hẹp lại.

8. **Danh mục mặc định là KHUÔN, không phải thứ người dùng nhìn thấy** (từ 2026-09-07). Mỗi tài khoản có **bản sao riêng** của bộ mặc định, do `DefaultCategorySeeder` tạo sau lần pull đầu; hàng toàn cục (`isDefault = true`, `idaccount = 0`) không còn hiện trong bất kỳ danh sách nào. Thiết kế: `docs/superpowers/specs/2026-09-07-per-account-default-categories-design.md`.
   - Luật tạo bản sao đếm **cả hàng đã xoá mềm**. Ba chữ ấy là khác biệt **duy nhất** với `ensureMissing()` cũ — thứ đã sinh ra G16. "Dọn dẹp" điều kiện ấy là tái hiện nguyên vẹn G16.
   - Nó chạy sau **mọi** lần pull chứ không phải một lần trong đời (đó là cách một danh mục mặc định thêm về sau tới được tài khoản đã seed), nên **tính luỹ đẳng là thứ tuyệt đối không được làm hỏng**.
   - ⚠️ **Hàng mặc định toàn cục nằm ở `idaccount = 0`** — `sync_engine.dart:733` quy `is_default = true` thành số 0 — nên mọi phép đọc chúng phải lọc theo **cờ `isDefault`**, không theo tài khoản. Bảng **tra tên** cho báo cáo/thống kê có một định nghĩa duy nhất ở **`CategoryDao.getBangTraTen` / `watchBangTraTen`** (giữ cả hàng đã xoá mềm **và** hàng mặc định); đừng viết truy vấn riêng. Thiếu vế `isDefault` thì giao dịch trỏ vào danh mục mặc định **mất tên**, hiện thành "Danh mục đã xoá" ở donut, thác nước, danh sách danh mục và chip Xu hướng — G41, đóng 2026-09-15; nó sống được lâu vì **hai** repository mỗi nơi một bản chép tay của cùng truy vấn, và chú thích ở cả hai đều đúng về vế kia (hàng đã xoá mềm).
   - `getNamesInUse` **vẫn đếm** hàng mặc định, và `purgeDataForOtherAccounts` **vẫn giữ** chúng. Hàm đầu phục vụ quy tắc trùng tên (hàng mặc định vẫn chiếm chỗ trong hai unique index của PostgreSQL); hàm sau giữ chính cái khuôn. Bỏ nhánh `idaccount = 0` ở hai chỗ ấy là hai kiểu hỏng khác nhau.
   - ⚠️ Bản sao chỉ đầy đủ khi **bộ mặc định cục bộ** đầy đủ. Pull là tăng dần theo `since`, nên một máy có thể chỉ biết một phần bộ mặc định của server. Từ 2026-09-07 server còn **13 hàng mặc định sống** (5 hàng `Chi khác`, `Thu khác`, `Làm thêm`, `Trả nợ`, `Thu nợ` đã bị **xoá mềm** khi backend thu bộ khuôn về đúng 13 stable UUID) — máy nào đã pull 5 hàng ấy sẽ nhận cờ xoá ở lượt pull sau. Đừng trông chờ con số khớp giữa hai máy.
   - **Màu danh mục đi qua đồng bộ bằng khoá `color`** (G24, đóng 2026-09-11). Server có cột `category.Color` (`varchar(9)`, từ `database/12`); `categoryForPush` đổi `colour` → `color` như `walletForPush` — một chỗ phủ cả thao tác danh mục đang chờ lẫn danh mục mà bước 1b của `_collectPendingOps` gửi kèm — nhánh kéo về đọc `c['color']` và rơi về màu cục bộ khi server trả `null`, và `sync_payload_contract_test.dart` khoá `'color'`. ⚠️ Danh mục đã `synced` từ trước chỉ lên màu khi được **lưu lại** (`updatedAt` mới hơn): đẩy cùng mốc `update_at` thì server trả xung đột và giữ bản của nó, nên bước 1b không tự bổ sung màu. Kiểm trên máy ảo 2026-09-11: lưu lại "Ăn uống" → `category.Color = #FF5722`. Tài liệu backend cũ: `docs/superpowers/backend/DA-XONG/CATEGORY_COLOUR_COLUMN.md`.

9. **Bảng `AppNotifications` là CỤC BỘ — đừng kéo nó vào đường đồng bộ.** Nó cố ý không có `syncStatus`/`syncError`/`updatedAt`/`isDeleted`; việc vắng mặt những cột đó chính là tài liệu sống. Thêm vào `SyncEntityType` là phải chạm bảy bảng ánh xạ song song phía backend mà **không được gì** — thông báo suy lại được từ ngân sách/hoá đơn/mục tiêu trên từng máy.
   - Tên bảng có tiền tố `App` vì Drift sinh data class **số ít** và `Notification` là lớp có thật trong `package:flutter/widgets.dart`. Cùng loại va chạm đã gặp với `Category` — xem dòng đầu `sync_engine.dart`.
   - ⚠️ Khi làm phần thông báo cấp hệ điều hành: `NotificationScanner.stop()` **phải** gọi `cancelAll()`. Lịch nằm trong AlarmManager/UNUserNotificationCenter chứ không trong SQLite, nên `purgeDataForOtherAccounts` không cứu được — nhắc hoá đơn của người đăng nhập trước sẽ nổ trên màn hình khoá của người sau.

---

## Lệnh hay dùng

```bash
# Test (chạy từ src/Client-app) — hiện **2854/2854** pass (đo 2026-09-18, 2 phút 36 giây, sau khi **đóng G46 — trần số chữ số thiếu ở BỐN mảng còn lại**: **16** ca mới ở **3** tệp, hai trong đó là tệp mới. Lỗ hổng của G45 **không phải chuyện riêng của ví** — tám cột tiền đều numeric(15,2), và giao dịch / hoá đơn / mục tiêu / ngân sách đều không giới hạn chữ số. Nặng nhất là **giao dịch**: ghi hàng ngày, bàn phím **tự vẽ** nên inputFormatters không với tới, lại có phím **000**; phép gõ nay tách thành hàm thuần. Có **test quét lib/ thứ tám** canh ô tiền mới. ⚠️ **Lần thứ BA trong ngày** một trần biến giá trị lớn nhất thành **hợp lệ đạt tới được** rồi lộ bố cục chưa từng thử: ở cỡ 48 con số dài nhất **ngắt hai dòng**, và đây **không phải tràn** nên takeException trả null — ca test phải đo **chiều cao thật**. Mốc **2838/2838** là cùng ngày, 2 phút 16 giây, sau khi **đóng G45 — sáu lỗ hổng của mảng ví**: **21** ca mới ở **5** tệp, trong đó `wallet_so_du_tran_test.dart` là tệp mới; **schema không đổi**, vẫn v23. ⚠️ Bảy ca trong số ấy sinh ra từ **hai lỗi do chính lượt vá tạo ra**, cả hai **chỉ máy ảo thấy** — tràn bố cục 70px, và cỡ chữ không co khi gõ. Mốc **2817/2817** là cùng ngày, 2 phút 41 giây, sau khi **trang Xuất báo cáo dùng chung bộ chọn kỳ với trang Phân tích** — mục **3.33** `ANALYTICS_FEATURE.md`: **9** ca mới, **7** ca của `khoangCuaPhamVi` bỏ theo hàm, **không** thêm tệp, **schema không đổi**, vẫn v23. Nay xuất được theo **tuần** và **năm**; `PhamViThoiGian` và `khoangCuaPhamVi` **bỏ hẳn**. ⚠️ Người dùng chốt **không** đưa khối nào của trang Phân tích vào tệp — khảo sát bảy app: **0/7** xuất cả màn phân tích ra tệp, **6/7** chỉ xuất CSV. ⚠️ Kèm **`nhanRong`**, hàm nhãn **thứ hai** cho chỗ không chật, sinh ra từ lỗi **chỉ máy ảo thấy**: nút nói "Tuần 37" trần còn dòng vừa chạm nói "Tuần 37 (07/09 – 13/09)". Mốc **2815/2815** là cùng ngày, 1 phút 42 giây, sau khi **đóng G44** — kỳ rỗng thì tệp xuất thôi in khối Ngân sách, mục **3.32** `ANALYTICS_FEATURE.md`: **5** ca mới ở **2** tệp, **không** thêm tệp, **schema không đổi**, vẫn v23. ⚠️ Cùng lượt, **ngoài phạm vi**: ba ca của `test/core/notification/os/os_notifier_native_test.dart` ghi **cứng** mốc `DateTime(2026, 9, 17, 21, 30)` và `DateTime(2026, 9, 20, 8)`, mà `flutter_local_notifications` gọi `validateDateIsInTheFuture` và **ném** với mốc đã qua — hai ca **đỏ từ sáng 18/09** ở vùng chẳng ai vừa đụng vào, làm cả bộ test đỏ vì một lỗi không liên quan. Nay dùng mốc **tương đối**; giờ 21:30 **giữ nguyên** vì đó là thứ bắt lỗi neo `TZDateTime` vào UTC. **Viết test lịch thì đừng ghi cứng một mốc tương lai** — nó là quả mìn hẹn giờ. Mốc **2810/2810** là của 2026-09-17, 2 phút 12 giây, sau **ba khối cuối vào tệp xuất báo cáo** — mục **3.31** `ANALYTICS_FEATURE.md`: **16** ca mới ở **1** tệp (`xuat_tep_test.dart`, **không** thêm tệp mới), **schema không đổi**, vẫn v23. ⚠️ Một trong 16 ca ấy **quét glyph** và sinh ra từ một lỗi đã chạy trong app từ 2026-09-09: Roboto nhúng không có `→` `▲` `▼`, gói `pdf` bỏ chúng đi im lặng. Mốc **2794/2794** là cùng ngày, 2 phút 30 giây, sau **G27 — cờ "ví được phép âm"** — 20 ca mới ở 4 tệp, trong đó `wallet_cho_phep_am_test.dart` là tệp mới; **schema v23** (cột `wallets.allow_negative`, migration chỉ `addColumn`). ⚠️ Cột mới làm data class Drift `Wallet` đòi thêm tham số bắt buộc, nên **5** chỗ dựng `Wallet(...)` bằng tay trong test và **3** fake repository implement `addWallet` phải sửa theo — việc cơ học, làm bằng máy. Mốc **2774/2774** là cùng ngày, 1 phút 49 giây, sau **khoản chi lớn** — loại thông báo thứ 17, mục #7 khảo sát lần hai: **31** ca mới ở 4 tệp, trong đó `notification_rules_large_expense_test.dart` là tệp mới; **schema không đổi**, vẫn v22. Mốc **2743/2743** là cùng ngày, 2 phút 17 giây, sau **tổng tài sản theo thời gian** — 41 ca mới ở 4 tệp, trong đó `tong_tai_san_test.dart` là tệp mới và **1** ca nằm ở `du_bao_dong_tien_test.dart` (siết `daiTrucDuBao`, bẫy 4.21 vế ba); **schema không đổi**, vẫn v22. ⚠️ Mốc **2742** đo cùng ngày là ảnh chụp **trước** lượt siết ấy — đừng dùng. Mốc **2702/2702** là của 2026-09-16, 2 phút 17 giây, sau **lịch chi tiêu** — 23 ca mới ở 3 tệp, trong đó `lich_chi_tieu_test.dart` là tệp mới; **schema không đổi**, vẫn v22. Mốc **2679/2679** là cùng ngày, 3 phút 20 giây, sau khi **đóng G43** — nút "Tuỳ chọn" của bộ chọn phạm vi chết im lặng: 9 ca mới ở 2 tệp, **không** thêm tệp, **schema không đổi**. Mốc **2670/2670** là cùng ngày, 1 phút 34 giây, sau **so cùng kỳ năm trước** — 27 ca mới ở 5 tệp, trong đó `moc_so_sanh_test.dart` là tệp mới; **schema không đổi**, vẫn v22. ⚠️ Bộ `analytics` cùng lượt đo được **19 tệp / 475 test**, tức +28 so với mốc "18 tệp / 447" ghi hôm trước — nhưng tôi chỉ thêm **27** ca và toàn bộ suite cũng chỉ +27, nên **mốc 447 ấy lệch một**, đúng 448 mới khớp. Đừng cộng dồn, hãy đếm lại. Mốc **2643/2643** là cùng ngày, 2 phút 48 giây, sau **dự báo dòng tiền 30 ngày** và **G42** — 70 ca mới ở 7 tệp, trong đó `du_bao_dong_tien_test.dart`, `bill_ky_ke_tiep_test.dart` và `bill_ky_ke_tiep_goi_lai_test.dart` là tệp mới; **schema không đổi**, vẫn v22. ⚠️ Trong 67 ca ấy có **7 ca `daiTrucDuBao`** sinh ra từ hai lỗi **chỉ máy ảo thấy** — trục từ 0 làm đường phẳng, và bước lẻ làm hai nhãn in đè (bẫy 4.21). Mốc **2573/2573** là của 2026-09-15, 2 phút 43 giây, sau khi **đóng G41** — danh mục mặc định toàn cục mất tên: 2 ca mới ở 2 tệp, **schema không đổi**. Mốc **2571/2571** là cùng ngày, sau **tỉ lệ tiết kiệm**: 12 ca mới ở 2 tệp, **schema không đổi**. Mốc **2559/2559** là cùng ngày, sau **dòng tiền tự do — A8 #8**: 19 ca mới ở 3 tệp, trong đó `dong_tien_tu_do_test.dart` là tệp mới và 5 ca cuối là widget test của trang; **schema không đổi**. ⚠️ Trong 19 ca ấy có **1 ca ở `thong_ke_thang_test.dart`** canh `rutGon` thôi in `-0` — lỗi máy ảo bắt được, không phải của riêng khối mới. Mốc **2540/2540** là cùng ngày, sau **thác nước "Tiền đi đâu" — A8 #10**: 22 ca mới ở 2 tệp, trong đó `thac_nuoc_test.dart` là tệp mới, **schema không đổi**; mốc **2518/2518** là cùng ngày, sau khi **nhãn quý rút thành `Q3 2026`** để ô header thôi cụt — 1 ca mới, **schema không đổi**; mốc **2517/2517** là cùng ngày, sau khi **đóng G40** — cột số tiền của trang Xem trước báo cáo thẳng mép phải: 6 ca mới ở 1 tệp, **schema không đổi**; mốc **2511/2511** là cùng ngày, sau **A8 #4 và #5 — hai biểu đồ cột vay/nợ**: 23 ca mới ở 3 tệp, trong đó `vai_vay_no_test.dart` là tệp mới, **schema không đổi**; mốc **2488/2488** là cùng ngày, sau khi **cột số tiền thẳng mép phải** ở hai khối của P2 — 1 ca mới so `getRect(...).right` giữa các dòng; mốc **2487/2487** là cùng ngày, sau **P2 — bốn khối mượn từ trang Báo cáo**: 20 ca mới ở 4 tệp, **schema không đổi**; mốc **2467/2467** là cùng ngày, sau **P1 — phạm vi thời gian trang Phân tích**: 58 ca mới ở 6 tệp, trong đó `pham_vi_ky_test.dart` và `chon_pham_vi_sheet_test.dart` là tệp mới; **schema không đổi**, vẫn v22; mốc **2409/2409** là 2026-09-14, sau khi đóng **G39** — nhãn trục tung của khối "Xu hướng 6 tháng" in đè hai chuỗi lên cùng một mốc, 1 ca mới; mốc **2408/2408** · 2 phút 15 giây là cùng ngày, sau lát A8 #3/#7 — 49 ca mới ở 5 tệp, trong đó `phan_loai_dong_tien_test.dart` là tệp mới; **schema không đổi**; mốc **2359/2359** là cùng ngày, sau đường tắt "Xem giao dịch" từ màn Quản lý ví — 3 ca mới; mốc **2356/2356** là cùng ngày, sau khi ba trang giao dịch thôi rơi về tài khoản admin — G38, 3 ca mới; mốc **2353/2353** là cùng ngày, sau khi `wallet.status` đi qua đồng bộ — G28 đóng, schema **v22**, 14 ca mới ở 4 tệp, trong đó 2 ca cuối do nghiệm thu máy thật sinh ra; mốc **2339/2339** là 2026-09-13, 2 phút 21 giây, sau khi số dư ví suy từ sổ giao dịch — G37 đóng, 34 ca mới ở 5 tệp, **schema không đổi**. Mốc 2305/2305 là cùng ngày, sau bước 12 — `auto_pay` qua đồng bộ và bốn lỗi im lặng mà nghiệm thu hai máy ảo bắt được; 9 ca mới ở 3 tệp, **schema không đổi**. Mốc 2296/2296 là cùng ngày, sau Task 8 và trước lượt nghiệm thu; mốc 2264/2264 là 2026-09-12 tối muộn, 2 phút 32 giây song song với `flutter analyze`, sau tính năng ân hạn hoá đơn — schema v21, 46 ca mới ở 12 tệp; mốc 2218/2218 là của tối cùng ngày, 4 phút 11 giây song song với `flutter analyze` và build APK, sau khi client nghe `sync.completed` — G34, 2 ca mới; mốc 2216/2216 là của cùng ngày chiều, 3 phút 36 giây song song với build APK, sau khi gỡ `SyncEngine.start()` khỏi `HomePage.build()` — 1 test quét mới; mốc 2215/2215 là sau khi client đọc `countdown` từ `/auth/profile` — 4 ca mới; mốc 2211/2211 là sau tính năng bỏ qua kỳ hoá đơn — 37 ca mới; mốc 2174/2174 là sau ba cột hoá đơn qua đồng bộ, 3 ca mới; mốc 2171/2171 là Phần 1 cưỡng chế đăng xuất + §5.1 và hai lượt sửa sau khi kiểm máy ảo, 66 ca mới; mốc 2105/2105 · 1 phút 7 giây là của cùng ngày, trước Phần 1). ⚠️ Thời gian dao động mạnh theo tải máy: 1 phút 36 giây khi chạy một mình, 3 phút 32 giây khi chạy song song với `flutter analyze`, tới 5,5 phút khi song song với build APK
flutter test
flutter analyze          # mức nền: 25 issue, KHÔNG có error

# Sau khi sửa Drift tables/DAOs (schema hiện tại: v23)
dart run build_runner build --delete-conflicting-outputs

# Chạy app
cd src/Backend && npm run dev                          # localhost:3000
cd src/Client-app && flutter run -d chrome --web-port 9090
```

**Trước khi báo là xong:** chạy `flutter test` và `flutter analyze`, đối chiếu với mức nền ở trên. Có lỗi mới phát sinh thì nói thẳng, đừng bỏ qua.

---

## Ghi chú vận hành

Những thứ dưới đây **đã từng gây thiệt hại thật**. Bốn mục đầu vốn chỉ nằm
trong file bàn giao tạm giữa các phiên nên chết đi sống lại nhiều lần; năm mục
cuối thêm ngày 2026-09-10, sau một phiên mà **lượt soát tìm ra lỗi trong chính
công việc vừa làm nhiều hơn trong tài liệu cũ**.

- ⚠️ **`mcp__stitch__edit_screens` KHÔNG nghiệm thu được bằng API — hỏi người
  dùng là phép đo duy nhất đáng tin.** Ngày 2026-09-14 nó trả về **thành công**
  kèm `dom_operations` liệt kê rõ từng `replace_element` với đủ `selector` và
  `screen_id` của màn được chọn, tức khẳng định đã sửa tại chỗ — mà thực tế
  **không đổi gì cả**, người dùng mở Stitch xem tận mắt cũng thấy y nguyên. Hai
  luật rút ra, cả hai đều phải trả giá bằng một lượt sửa tài liệu: (1) kết quả
  trả về **không** chứng minh công cụ đã làm gì; (2) một màn mới xuất hiện
  **không** chứng minh lời gọi của mình tạo ra nó — người dùng ngồi trước Stitch
  và thao tác song song mà mình không thấy (màn `c8567243…` chính là do họ bảo
  Stitch tạo, sau khi tôi đã kết luận nhầm là của mình). Quy trình vẫn nên là
  `list_screens` trước và sau để biết danh sách có đổi không, nhưng **đừng viết
  nguyên nhân vào tài liệu** — chỉ ghi cái đo được, rồi hỏi.
- ⚠️ **Nhưng đừng suy ngược thành "cứ nghi ngờ là xong".** Ngày 2026-09-15
  `generate_screen_from_text` **trả về `timeout`**, và lượt `list_screens` ngay
  sau đó không thấy màn nào mới — tôi ghi nhận "chưa đổi nghĩa là chưa biết" rồi
  làm việc khác. **Hơn một tiếng sau màn ấy có thật**, đúng nội dung đã yêu cầu
  (`83993fc9…` *"Thống kê - Chọn phạm vi thời gian"*), và người dùng xác nhận nó
  do lượt gọi ấy tạo ra. Tức **timeout không phải thất bại**, và **đừng gọi
  lại** — tài liệu công cụ dặn *"DO NOT RETRY"*, còn gọi lại sớm thì dự án lãnh
  thêm một màn trùng. Hai bài học ở trên và bài học này kéo về hai phía ngược
  nhau, nên phép đo vẫn chỉ có một: **hỏi người dùng**.
- ⚠️ **Và nó còn có ĐỘ TRỄ DÀI — "chưa đổi" nghĩa là *chưa biết*, không phải
  *thất bại*** (2026-09-13, dự án `FlowMoney`).
  Thay đổi có thể mất **hàng chục phút** mới hiện ở API (`get_screen` trả
  `htmlCode.name` và `screenshot.name` mới) lẫn ở giao diện Stitch. Suốt khoảng
  trễ ấy mọi dấu hiệu đều nói "không có gì thay đổi": HTML tải về **giống bản
  gốc từng byte**, cả hai ID đều y nguyên. Tôi đã dựa đúng vào những dấu hiệu đó
  để kết luận công cụ hỏng, viết vào ba tài liệu rồi commit — người dùng kiểm
  lại sau đó và thấy màn **đã** sửa xong; phải gỡ lại toàn bộ. Nên: gọi xong thì
  **chờ và làm việc khác**, kiểm lại sau, và khi nghi ngờ thì **hỏi người dùng
  nhìn giúp trên Stitch** thay vì tự chốt. Chỉ ghi kết luận vào tài liệu khi nó
  đã ổn định qua nhiều lượt đo cách nhau. Cách nghiệm thu vẫn đúng: tải
  `htmlCode.downloadUrl` về đọc, và so `screenshot.name` trước/sau. Một lượt còn
  **timeout** — tài liệu công cụ dặn *"DO NOT RETRY"*; ở đây kiểm trước rồi mới
  thử lại là đúng, nhưng nhớ rằng phép kiểm ấy cũng chịu cùng độ trễ.
- ⚠️ **Đọc màn Stitch thì phải đọc đúng KHỐI, đừng `grep` một từ.** Cùng lượt
  trên, cả bốn màn đều **có** chữ "Kỳ" — nhưng ở phần `LỊCH SỬ CÁC KỲ`, không
  phải dòng "Kỳ" của khối `THÔNG TIN` đang thiếu. `grep` một chữ rồi kết luận
  "đã có" là sai. Cách rẻ mà đúng: tải HTML về, thay thẻ bằng `|`, rồi in đoạn
  ngay sau tên khối.

- **Đừng ngắt `flutter test` giữa chừng, và đừng chạy hai lần cùng lúc.**
  `flutter_tester.exe` mồ côi giữ `build/native_assets/windows/sqlite3.dll`,
  mọi lần chạy sau nổ `PathExistsException` cho tới khi tắt hết tester và xoá
  thư mục ấy. Muốn chặn treo thì chạy nền ghi log + `--timeout 60s`. Widget
  test treo đủ 10 phút/test mà `--timeout` không cắt được là dấu hiệu
  `bloc.close()` chờ một stream không bao giờ `done` — thăm dò bằng một
  `test()` thường bọc `FakeAsync().run(...)`, chạy đồng bộ nên không thể treo
  (đã vấp 2026-09-06 với `asyncMap` trên `Stream.value`).

- **Bash tool ở đây là Git Bash, không phải PowerShell.** Commit message nhiều
  dòng thì dùng `git commit -F -` với heredoc `<<'EOF'`. Here-string
  `@'...'@` của PowerShell **làm lọt ký tự `@` vào message mà không báo lỗi** —
  đã phải `reset --soft HEAD~3` và commit lại cả ba lần.

- **Ghi file dài thì dùng công cụ Write, đừng qua `cat > file <<'EOF'`.** Nội
  dung nhiều backtick hoặc dấu nháy làm shell hiểu sai và chết với
  `unexpected EOF`, dù heredoc đã được trích dẫn.

- **Truy vấn PostgreSQL: máy này không có `psql`,** nhưng chạy được qua Prisma.
  Phải chạy **từ `src/Backend`** thì Node mới phân giải được `@prisma/client` —
  đặt script ở `/tmp` sẽ báo `MODULE_NOT_FOUND`:
  ```bash
  cd src/Backend && node -e "const {PrismaClient}=require('@prisma/client');
  const p=new PrismaClient();(async()=>{console.log(await p.\$queryRawUnsafe('SELECT 1'));
  await p.\$disconnect();})();"
  ```
  ⚠️ Chỉ dùng truy vấn **đọc**. Xoá cứng ở PostgreSQL vi phạm quy tắc 5.

- **Áp tệp `database/N_*.sql` và `prisma generate` trên máy này** (đã làm tối
  2026-09-10 cho 7–11, ngày 2026-09-11 cho 12 và 2026-09-12 cho 13, cả ba theo yêu cầu đích danh — banner
  đầu `DA-XONG/DEV_DB_MIGRATIONS_7_11.md`, khối "Áp `database/12`" và khối "Gộp `main` @ `cbbeeb4`…" mục 14
  `PROJECT_CONTEXT.md`). Script đặt ở scratchpad vẫn chạy được nếu nạp gói bằng
  `require(require.resolve('pg', { paths: [process.cwd()] }))` và chạy với thư mục làm
  việc là `src/Backend`.
  SQL nhiều câu chạy bằng gói `pg` từ `src/Backend` — `$executeRawUnsafe` của
  Prisma không nhận nhiều câu một lần — mỗi tệp một giao tác. `generate` phải tắt
  **cả cây** `npm run dev` → `cmd` → `nodemon` → `node index.js` trước (tệp engine
  bị khoá), không chỉ tiến trình `node`, nếu không lần chạy lại thành hai
  `nodemon`. ⚠️ Backend chạy nền bằng công cụ Bash **không** ghi log console ra
  tệp output (đo: 0 dòng) — đừng dựa vào nó để soát dòng `WARN`.

- **Repo đặt `core.autocrlf=true` và không có `.gitattributes`.** Sửa file bằng
  script Python (ghi ra LF) sẽ khiến git cảnh báo `LF will be replaced by CRLF`.
  Vô hại, nhưng nhớ kiểm `git diff --stat` để chắc không bị nhiễu toàn file.

---

- **Đừng lọc kết quả một phép ĐO qua `head`/`tail`.** Ngày 2026-09-10 một lượt
  đo `pg_constraint` bị `tail -25` cắt mất bốn dòng đầu, và tôi kết luận bảng
  `wallet` "không có CHECK constraint nào" — sai hoàn toàn, bảng có 18 ràng buộc
  đủ cả bốn `chk_wallet_*`. Kết luận sai ấy đã kịp đi vào `CLAUDE.md` và
  `PROJECT_CONTEXT.md` trước khi lượt soát bắt được. Lọc bằng **dấu hiệu từng
  dòng** (`console.log('ROW| ...')` rồi `grep "^ROW|"`), hoặc in ra tệp rồi đọc.
  Đây là **lần thứ hai** output bị cắt làm sai kết luận — lần trước là `grep` qua
  rtk. Khi kết quả trông đáng ngờ, kiểm lại bằng Python thay vì tin `grep`.

- **Mọi con số trong tài liệu phải ĐẾM BẰNG SCRIPT, và ghi kèm ngày đếm.** Cùng
  phiên ấy tôi viết "ba cột" rồi liệt kê hai, và chép lại con số "14 chỗ gọi"
  đã cũ. Dự án đã có nếp "đếm bằng máy" ở nhiều chỗ — theo nó, đừng đếm bằng mắt.

- **Trước khi commit một hạng mục, quét API MỚI THÊM có 0 chỗ gọi.** Ngày
  2026-09-10 tôi thêm `WalletDao.watchActive` vào cả DAO lẫn datasource mà không
  chỗ nào cần — chỉ lộ ra khi đếm chỗ gọi để sửa một con số trong tài liệu.

- **Đổi một quyết định thì grep theo TỪ KHOÁ CỦA QUYẾT ĐỊNH CŨ, không chỉ sửa chỗ
  vừa đụng.** Khi `wallet.status` chuyển thành cột cục bộ, ba chú thích ở ba tệp
  khác vẫn nói nó "đi ra máy khác qua `/sync/push`". Chúng chỉ lộ ra khi grep
  đúng cụm chữ ấy. ⚠️ **Và điều y hệt lặp lại theo chiều ngược ngày 2026-09-14**,
  khi G28 mở cột ấy trở lại: lượt grep `"cột cục bộ"` bắt được một chú thích ở
  `dieu_chinh_so_du.dart` và một dòng trong `SYNC_DOCUMENTATION.md` (payload ví
  "12 trường") — cả hai đều ở tệp mà hạng mục **không hề chạm tới**. Bài học
  không phải "nhớ dọn khi tắt một tính năng" mà là **mỗi lần đổi một quyết
  định, theo cả hai chiều**.

- **Lượt soát tài liệu phải quét cả tài liệu KHÔNG do mình sửa.** Soát bốn tệp
  vừa chạm là chưa đủ: lượt quét rộng ngày 2026-09-10 bắt được một mục của phiên
  trước nói ngược mục mới ngay bên trên nó, một dòng tổng kết lỗ hổng bỏ sót bốn
  mục, và bảng tóm tắt của `CLIENT_APP_KNOWN_GAPS.md` **trôi khỏi thân lần thứ
  năm**. Cách rẻ: `grep` tên tính năng và tên hằng số vừa đổi trên toàn `docs/`.

## Ghi chú về kiểm thử

Bộ test là lưới an toàn chính của dự án này — nhiều lỗi trong quá khứ hỏng **âm thầm** (không exception, không log). Khi sửa lỗi, viết test tái hiện **trước**, và ghi rõ trong `reason:` của assertion là nó canh chừng điều gì.

Vùng chưa có test nào: feature `ai_chat` (`profile` nay có test — `test/features/profile/` gồm **2** tệp / **9** test, từ G33 — đếm bằng máy 2026-09-11). (`wallet` có **26** tệp / **162** test — đếm bằng chính `flutter test test/features/wallet` ngày 2026-09-18, sau khi đóng **G45**; tệp mới của lượt ấy là `wallet_so_du_tran_test.dart`. Mốc **23 tệp / 128 test** ngay sau đây là của 2026-09-13, giữ làm ảnh chụp) (dưới `test/features/wallet/` kể cả `domain/` (đếm bằng chính `flutter test test/features/wallet` 2026-09-13, sau khi số dư ví suy từ sổ — bốn tệp mới: `domain/so_du_mo_so_test`, `so_du_vi_service_test`, `so_du_mot_noi_ghi_test`, `wallet_tao_moi_sinh_neo_test`; mốc **19 tệp / 106 test** là của 2026-09-11)— đếm bằng chính `flutter test test/features/wallet` 2026-09-11, sau khi gỡ luật một ví Tiết kiệm (G30: bỏ năm ca của hàm đã xoá, nhóm năm ca ở datasource thay bằng ba ca "nhiều ví Tiết kiệm"); con số "113" ghi ở đây trước đó đúng tới trước bản ấy, "109" trước nữa không khớp phép đếm, và "13 tệp / 62 test" đếm theo cách khác. Ba tệp cho ràng buộc ví của server thêm ngày 2026-09-10 (khi ấy là hai luật): `domain/rang_buoc_vi_test`, `wallet_unique_constraints_test`, `wallet_add_guard_ui_test`. Sáu tệp — dòng này từng ghi "năm" dù liệt kê sáu — thêm cùng ngày cho tính năng lưu trữ ví (`domain/wallet_status_test`, `domain/vi_tinh_vao_tong_test`, `wallet_archive_test`, `wallet_archive_ui_test`, `wallet_edit_archive_switch_test`, `wallet_picker_sources_test`); `core/utils/gioi_han_do_dai_test.dart` có **12** test cho bộ lọc độ dài tên (G31); `core/utils/currency_formatter_test.dart` có **17** test, trong đó **một test quét cả `lib/`** cấm dựng `NumberFormat` ngoài `CurrencyFormatter`; **`wallet_picker_sources_test.dart` là test quét `lib/` thứ hai** — nó bắt mọi chỗ đọc danh sách ví phải được phân loại tay là *bộ chọn ví* hay *bảng tra tên*, vì chọn nhầm hỏng im lặng. **`core/auth/khong_du_phong_admin_test.dart` là test quét `lib/` thứ ba** — cấm mã tài khoản rơi về `?? 1` gần mã tài khoản/người dùng, kể cả khi biểu thức bị ngắt dòng (G35, 2026-09-11: ba chỗ ở màn quản lý danh mục lọt qua mọi lượt `grep` một dòng vì dạng `?? ⏎ 1;`). ⚠️ Tệp này có **hai** ca từ 2026-09-14: ca thứ hai cấm một **tham số** tên chứa `account`/`user` khai mặc định `= 1`, vì ba trang giao dịch né được ca đầu bằng cách viết `?? widget.idaccount` — vế `??` trỏ tới biến chứ không tới hằng, nên grep `?? 1` sạch trong khi route dựng `const TransactionPage()` vẫn dùng đúng mã admin (G38). **`core/sync/sync_engine_start_owner_test.dart` là test quét `lib/` thứ tư** (2026-09-12) — chỉ `auth_bloc.dart` được gọi `.start(idaccount:)`; sinh ra khi gỡ bản trùng `SyncEngine.start()` khỏi `HomePage.build()`, nơi mỗi lần dựng lại là một chu kỳ đồng bộ thừa và giãn cách lùi bị xoá. **`features/bill/bill_conflict_resolver_wiring_test.dart` là thứ **năm**** (2026-09-13) — `BillPaymentConflictResolver` phải được **bắt đầu nghe** chứ không chỉ được dựng; ⚠️ test quét phải **loại trừ chính tệp định nghĩa**, nếu không nó đếm cả dòng khai báo và xanh sai. **`features/wallet/so_du_mot_noi_ghi_test.dart` là thứ **sáu**** (2026-09-13) — chỉ `SoDuViService` được gọi `updateBalance`; số dư ghi sai không gây lỗi nào, nên một chỗ cộng dồn sót lại chỉ hiện ra bằng một con số sai mà không ai lần được từ đâu. Ba tệp ví ở `core/database/` (`wallet_schema_v20_test.dart`, `wallet_order_test.dart`, `wallet_archive_dao_test.dart`) có **17** test — đếm bằng máy 2026-09-10.) (`core/realtime/` có **5** tệp test / **40** test — đếm bằng máy 2026-09-12 tối, sau ca `sync.completed` của G34; mốc 39 là cùng ngày sau `realtime_buoc_dang_xuat_test` (7 ca) của Phần 1; mốc 4 tệp / 32 test là của 2026-09-09.) (`analytics` có **21** tệp test / **571** test — đếm bằng chính `flutter test test/features/analytics` ngày 2026-09-18 sau khi **trang Xuất báo cáo dùng chung bộ chọn kỳ** (9 ca mới, 7 ca bỏ, **không** thêm tệp). Mốc **569** là cùng ngày, sau khi **đóng G44** (5 ca mới, **không** thêm tệp — nên số tệp **giữ nguyên 21**; 3 ca ở `xuat_tep_test.dart`, 2 ca ở `bao_cao_xuat_test.dart`). Mốc **564** là của 2026-09-17 sau **ba khối cuối vào tệp xuất** (16 ca mới, tất cả ở `xuat_tep_test.dart`, **không** thêm tệp — nên số tệp **giữ nguyên 21**). Mốc **548** là cùng ngày sau **tổng tài sản theo thời gian** (tệp mới `tong_tai_san_test.dart`, 26 ca; cộng 7 ca ở `analytics_repository_impl_test.dart`, 7 ca widget ở `analytics_page_test.dart`, và 1 ca ở `du_bao_dong_tien_test.dart`). Mốc **20** tệp / **507** test — đếm bằng chính `flutter test test/features/analytics` ngày 2026-09-16 sau **lịch chi tiêu** (tệp mới `lich_chi_tieu_test.dart`, 15 ca; cộng 2 ca ở `analytics_repository_impl_test.dart` và 6 ca widget ở `analytics_page_test.dart`). Mốc **19 tệp / 484 test** là cùng ngày sau khi **đóng G43** (5 ca ở `pham_vi_ky_test.dart` cho `khoangKhoiTaoBoChonNgay`, 4 ca widget ở `chon_pham_vi_sheet_test.dart` chạm **thật** vào nút "Tuỳ chọn"). Mốc **475** là cùng ngày sau **so cùng kỳ năm trước** (tệp mới `moc_so_sanh_test.dart`, 7 ca; cộng 9 ca ở `pham_vi_ky_test.dart`, 2 ở `analytics_repository_impl_test.dart`, 4 ở `analytics_cubit_test.dart`, 5 ở `analytics_page_test.dart`). ⚠️ Tổng cộng tôi thêm **27** ca và cả suite cũng chỉ +27, nên mốc **447** ghi trước đó **lệch một** — 448 mới khớp. Mốc **18 tệp / 447 test** là cùng ngày sau **dự báo dòng tiền 30 ngày** (tệp mới `du_bao_dong_tien_test.dart`, 49 ca — trong đó 7 ca `daiTrucDuBao` do máy ảo sinh ra; cộng 8 ca widget ở `analytics_page_test.dart` và 4 ca ở `analytics_repository_impl_test.dart`). ⚠️ Bộ hoá đơn cùng lượt lên **289** ca (hai tệp mới `bill_ky_ke_tiep*`). Mốc **17 tệp / 386 test** là 2026-09-15 sau khi **đóng G41** (2 ca mới, **không** thêm tệp); mốc **384** là cùng ngày sau **tỉ lệ tiết kiệm** (12 ca mới, **không** thêm tệp); mốc **372** là cùng ngày sau **dòng tiền tự do** (tệp mới `dong_tien_tu_do_test.dart`, 13 ca; cộng 5 ca widget ở `analytics_page_test.dart` và 1 ca `rutGon` ở `thong_ke_thang_test.dart`); mốc **16 tệp / 353 test** là cùng ngày sau thác nước (tệp mới `thac_nuoc_test.dart`, 18 ca; cộng 4 ca widget ở `analytics_page_test.dart`); mốc **331** là cùng ngày sau khi sửa nhãn quý (1 ca mới ở `pham_vi_ky_test.dart`); mốc **330** là cùng ngày sau khi đóng G40 (6 ca mới ở `report_preview_page_test.dart`, **không** thêm tệp); mốc **324** là cùng ngày sau A8 #4/#5 (tệp mới `vai_vay_no_test.dart`); mốc **14 tệp / 301 test** là cùng ngày sau lượt sửa cột số tiền thẳng mép; mốc **300** là cùng ngày sau P2 bốn khối mượn từ trang Báo cáo; mốc **283** là cùng ngày sau P1 phạm vi thời gian (hai tệp mới `pham_vi_ky_test.dart` và `chon_pham_vi_sheet_test.dart`); mốc **12 tệp / 229 test** là 2026-09-14 sau lát A8 #3/#7 (tệp mới `phan_loai_dong_tien_test.dart`); mốc **11 tệp / 180 test** là của cùng ngày trước lát ấy, mốc **10 tệp / 170 test** là của 2026-09-09 sau 6 tệp của lát 2c, nó thiếu `khoan_vao_thong_ke_test.dart` thêm ngày 2026-09-13 cùng G37). (`notification` có **22** tệp dưới `test/core/notification/` + `test/features/notification/` — 14 ở gốc, 5 trong `os/` và `prefs/`, 3 ở `features/` — cộng 3 tệp liên quan nằm chỗ khác. Đếm lại bằng máy 2026-09-09 sau khi thêm `tuan_iso_test` và `notification_rules_weekly_test`; `home` có test từ 2026-09-06.) (`core/api/` có **3** tệp / **39** test — `auth_interceptor_test` 17, `ket_qua_lam_moi_test` 13, `auth_interceptor_buoc_dang_xuat_test` 9 — đếm bằng máy 2026-09-12, sau Phần 1; mốc 2 tệp / 30 test là của cùng ngày, trước Phần 1. `core/auth/` có **4** tệp / **38** test và `test/features/auth/` có **9** tệp / **72** test (đếm bằng máy 2026-09-12 chiều, sau 4 ca `countdown` từ `/auth/profile`; 68 là mốc trước đó, 65 là mốc trước hai lượt sửa sau khi kiểm máy ảo); `budget` có test từ 2026-09-03.)

### ⚠️ Ba loại lỗi mà `flutter test` KHÔNG bắt được

Phát hiện ngày 2026-09-04 khi chạy app trên máy ảo Android. Cả ba đều để bộ test xanh, nên **đụng vào giao diện hoặc điều hướng thì phải chạy trên máy ảo trước khi báo xong** — `flutter test` và `flutter build web` không thay thế được.

1. **Tràn bố cục.** Bộ test và skill `chay-app` chạy Chrome ở **1280px**, còn điện thoại thật là **411dp** — rộng gấp ba. Tìm được ba chỗ tràn (21px, 3,9px, 0,315px) đã nằm sẵn trong mã từ lâu. Muốn test được thì phải **trích widget ra** rồi dựng trong `SizedBox` hẹp có chủ ý, và bắt bằng `tester.takeException()`: Flutter báo lỗi tràn qua `FlutterError.reportError` chứ **không ném ra chỗ gọi**, nên test chỉ `pumpWidget` + `expect(find...)` sẽ xanh ngay cả khi màn hình đầy sọc cảnh báo.

2. **Điều hướng qua `StatefulShellRoute`.** `push` một route nằm trong shell từ một trang ngoài shell làm app **chết màn đỏ** (`!keyReservation.contains(key)`). Chỉ nổ khi có cây route thật. Xem bẫy 7.8 `docs/NOTIFICATION_FEATURE.md`.

3. **Thứ tự thực tế giữa hai luồng bất đồng bộ.** Hai stream có thể đều đúng khi test riêng, nhưng trên máy thật cái này ghi đè cái kia. Ví dụ đã gặp: `SyncEngine` đẩy xong sau 0,4 giây còn bộ theo dõi kết nối báo ở giây thứ 3, nên dải "đã đồng bộ" bị dải "đã kết nối lại" nuốt mất.

**Chạy máy ảo:** khởi động bằng **`emulator -avd <tên> -gpu swangle`**, rồi `flutter build apk --debug`, `adb install -r build/app/outputs/flutter-apk/app-debug.apk` và `adb shell am start -n com.flowmoney.flowmoney/.MainActivity`. ⚠️ **Cờ `-gpu` quyết định máy ảo có sống nổi khi người dùng bấm vào cửa sổ hay không** (đo 2026-09-14): với `-gpu swiftshader_indirect` máy ảo chết **ba lần** với exit 139 (SIGSEGV) đúng lúc người dùng thao tác trực tiếp, trong khi mọi lệnh `adb` của Claude chạy suốt phiên không sao — đổi sang **`swangle`** thì **hết hẳn**, người dùng xác nhận. Emulator trên máy này là **36.4.10**, đã **bỏ** `angle_indirect` (báo *not valid* rồi âm thầm rơi về `auto` — tức dùng GPU thật, ngược hẳn ý định); các giá trị còn nhận: `auto`, `host`, `software`, `lavapipe`, `swiftshader`, `swangle`. ⚠️ Dòng `Warning: Software OpenGL failed. Falling back to system OpenGL` trong log **luôn xuất hiện** ở mọi chế độ `-gpu` — nó nói về **lớp cửa sổ Qt**, không phải về render của máy Android, và nguyên nhân là `opengl32sw.dll` không có trong bản cài SDK. Đừng dùng dòng ấy để đoán cờ `-gpu` có hiệu lực hay không — đọc `emuglConfig_init: ... gles_mode_selected:` mới đúng. ⚠️ `adb` **không có trong PATH** — dùng `%LOCALAPPDATA%/Android/Sdk/platform-tools/adb.exe`. ⚠️ Máy ảo có **dữ liệu di động** (`settings get global mobile_data` = 1): `svc wifi disable` **không cắt mạng**, chỉ đổi đường — muốn mất mạng thật phải `svc data disable` cùng lúc (vấp 2026-09-12: tưởng socket đã đứt mà nó vẫn nhận sự kiện). Đăng xuất nhanh không qua giao diện: `am force-stop` rồi `run-as com.flowmoney.flowmoney rm shared_prefs/FlutterSecureStorage.xml` (kho token; SQLite không đụng). Ép token hết hạn để đo interceptor: hạ `JWT_USER_ACCESS_EXPIRES` ở `.env` backend dev — đụng `src/Backend`, cần cho phép đích danh, và **trả lại ngay** sau khi đo.

**Mẹo dò tràn hàng loạt:** sọc cảnh báo của Flutter là **vàng thuần** và không màn nào của app dùng màu ấy, nên đếm pixel vàng trong ảnh `adb exec-out screencap` rẻ hơn hẳn việc mở từng ảnh ra nhìn. Cẩn thận dương tính giả với màn hình launcher của Android.
