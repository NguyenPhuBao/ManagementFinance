<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Thông báo nhắc hoá đơn / cảnh báo ngân sách.
         Từ Android 13 (API 33), thiếu POST_NOTIFICATIONS thì thông báo bị nuốt
         hoàn toàn — không lỗi, không log. Quyền được xin có ngữ cảnh trong app
         (LocalOsNotifier.requestPermission), không phải lúc khởi động.
         RECEIVE_BOOT_COMPLETED để plugin đặt lại lịch sau khi máy khởi động
         lại — nếu không, mọi nhắc hoá đơn biến mất sau một lần tắt máy.

         CỐ Ý KHÔNG khai SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM: Google Play
         chỉ chấp nhận chúng cho nhóm báo thức/lịch, và trên Android 14 người
         dùng còn phải tự bật trong Cài đặt. Nhắc hoá đơn lệch mươi phút không
         ảnh hưởng gì, nên lịch dùng AndroidScheduleMode.inexactAllowWhileIdle.
         "Nhắc hoá đơn thì nên chính xác" nghe rất hợp lý — đừng đổi, sẽ bị Play
         từ chối lúc phát hành. -->
    <!-- ⚠️ INTERNET phải khai Ở ĐÂY, không chỉ ở debug/AndroidManifest.xml.
         Flutter tạo sẵn quyền này trong manifest của bản debug (để hot reload
         nối được), nên mọi bản debug gọi backend bình thường và thiếu sót ở
         main KHÔNG lộ ra ở đâu cả: `flutter test`, `flutter analyze` và cả
         `flutter build apk` đều xanh. Nó chỉ hiện khi CHẠY một bản release —
         và khi ấy app không gọi được bất cứ đâu, chỉ hiện "Không có kết nối
         mạng" y như lúc rớt sóng. Đo thật 2026-09-22 trên OnePlus 13R: bản
         release đầu tiên của dự án không đăng nhập được, trong khi `curl` từ
         shell của chính máy ấy tới cùng URL trả về 200. Có test đọc tệp này
         canh (`test/core/nhan_dien_app_test.dart`). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- ⚠️ Tải mô hình chạy nền. `background_downloader` khai service của nó
         với android:foregroundServiceType="dataSync", và từ Android 14 (API 34)
         mỗi loại foreground service đòi một quyền riêng — dự án targetSdk 36
         nên đây là bắt buộc. Thiếu nó thì `flutter test`, `flutter analyze` và
         `flutter build apk` ĐỀU XANH; lỗi chỉ hiện khi lượt tải thật cố lên
         foreground trên máy Android 14+. Cùng vùng mù đã cho lỗi "APK release
         thiếu INTERNET" ngày 2026-09-22. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Predictive back TẮT (2026-09-19, UX E3): targetSdk 36 bật nó mặc định,
         và khi bật thì Android chỉ giao Back cho Flutter nếu lời gọi
         setFrameworkHandlesBack cuối cùng là true - mà Navigator của nhánh
         StatefulShellRoute mới dựng phát canHandlePop:false, listener của
         navigator gốc cho qua (chỉ hỏi canPop(), không hỏi PopScope), nên Back
         ở tab Phân tích đóng thẳng activity. Tắt thì Back đi đường KEYCODE_BACK
         -> popRoute -> PopScope của shell. Có test đọc tệp này canh. -->
    <application
        android:enableOnBackInvokedCallback="false"
        android:label="FlowMoney"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- BA receiver của flutter_local_notifications. Thiếu cái nào cũng
             hỏng im lặng — không exception, không log, không một dòng trong
             logcat — nên `android_manifest_receivers_test.dart` canh cả ba.
             Lịch đặt trước vẫn "đặt thành công" nhưng KHÔNG BAO GIỜ nổ nếu
             thiếu hai cái đầu; nút hành động vẫn HIỆN nhưng bấm không tới ai
             nếu thiếu cái thứ ba. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <!-- Nhận cú bấm nút hành động khi app đã đóng, rồi khởi động isolate
             nền gọi `khiChamNutLucAppDong`. Plugin KHÔNG tự khai báo nó; đây
             là bước phải làm tay, và đó chính là lý do nó dễ bị bỏ sót. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
