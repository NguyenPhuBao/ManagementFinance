import 'package:flutter/material.dart';

class WalletAddPage extends StatelessWidget {
  const WalletAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Thêm ví')),
    );
  }
}

