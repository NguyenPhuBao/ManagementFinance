import 'package:flutter/material.dart';

class BillAddPage extends StatelessWidget {
  const BillAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Thêm hóa đơn')),
    );
  }
}

