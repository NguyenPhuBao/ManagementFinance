import 'package:flutter/material.dart';

class GoalAddPage extends StatelessWidget {
  const GoalAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Thêm mục tiêu')),
    );
  }
}

